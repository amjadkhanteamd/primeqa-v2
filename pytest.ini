[pytest]
testpaths = tests/unit tests/integration
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests requiring database connection (slower)
    unit: pure-function tests with no I/O (fast)
addopts =
    -ra
    --strict-markers
