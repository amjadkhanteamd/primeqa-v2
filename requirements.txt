flask>=3.0
sqlalchemy>=2.0
psycopg2-binary>=2.9
PyJWT>=2.8
bcrypt>=4.0
python-dotenv>=1.0
cryptography>=41.0
requests>=2.31
gunicorn>=21.2
apscheduler>=3.10
croniter>=2.0
anthropic>=0.25
pgvector>=0.3
numpy>=1.26
PyYAML>=6.0
alembic>=1.13
httpx>=0.27,<1.0

# Phase 5 Part 1: EN 301 549 PDF text extraction for the criterion-catalogue ingester (pinned — extraction determinism is part of ingest reproducibility)
pypdf==6.16.2

# Evidence-link minting on the web tier: ui_report's evidence fragment builds the same store client the browser-worker uses (same pin as requirements-browser.txt)
boto3==1.43.78
