[
 {
  "id": "AUD-001",
  "title": "authz oracle misreports three authenticated routes as open: it does not resolve the `_require_auth_api` alias",
  "class": "self-reporting control: the gate oracle reads decorator NAMES, not the decorators that run",
  "severity": "medium",
  "pass": 1,
  "surface": "scripts/authz_inventory.py (the D-245 regression oracle)",
  "reproduction": "python scripts/authz_inventory.py | grep '(none'",
  "evidence": "/api/s3-generation-jobs/<id>, /api/s4-execution-jobs/<id>, /api/s3-generation-jobs/<id>/cancel listed as '(none \u2014 auth/login only or open)'; views.py:245 `from primeqa.core.auth import require_auth as _require_auth_api`; the three routes carry @_require_auth_api",
  "expected": "the oracle names the gate that actually runs on every route",
  "observed": "any alias of a gate is invisible to it; the oracle can be green while a route is open, or red while it is gated \u2014 its report is not evidence of either",
  "proposed_fix": "derive gates from the live app (walk __wrapped__ / closure args) or resolve import aliases in the AST; add a guard asserting the oracle and the live map agree",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:34+00:00"
 },
 {
  "id": "AUD-002",
  "title": "the template dead-link sweep silently excludes every href that carries a Jinja expression",
  "class": "a sweep that skips what it cannot parse reports the residue as clean",
  "severity": "medium",
  "pass": 1,
  "surface": "tests/unit/test_step_6a_routes.py::test_no_template_links_to_a_path_that_does_not_answer",
  "reproduction": "sed -n 63p tests/unit/test_step_6a_routes.py \u2014 href regex is /[^\"{}\\s]*/, so any href containing `{` is never checked; scripts/audit/deadlinks.py url_map.json finds what it skipped",
  "evidence": "166 internal targets in templates+JS; the unit sweep can see at most the 60 with no Jinja; 5 dead targets found, 2 on live pages (AUD-003, AUD-004); the sweep also ignores action=, hx-*, data-confirm-form and fetch() literals",
  "expected": "a dead-link sweep resolves every target a browser or htmx will follow, parameterised or not",
  "observed": "the sweep has been green through D-490, D-491 and the retirement while two dead links sat on live pages",
  "proposed_fix": "replace the sweep's regex with scripts/audit/deadlinks.py's resolver (all attributes, Jinja collapsed to a wildcard segment, if-branches expanded) and gate on zero dead",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:35+00:00"
 },
 {
  "id": "AUD-003",
  "title": "release detail: 'View Reasoning' links to /releases/<id>/decision, a route that does not exist",
  "class": "dead link on a live page (link retired, anchor kept)",
  "severity": "medium",
  "pass": 1,
  "surface": "/releases/<id> \u2014 templates/releases/detail.html:74, inside the latest-decision block",
  "reproduction": "open any release that holds a decision (production release 16 holds decision 72); click 'View Reasoning \u2192'",
  "evidence": "templates/releases/detail.html:74 href=\"/releases/{{ release.id }}/decision\"; url_map has no /releases/<int>/decision rule (only evaluate-decision, plan, run, targets, waivers); the decision lives at ?tab=decision",
  "expected": "the link opens the decision tab",
  "observed": "404 on the programme's most-viewed page, for exactly the releases that have a decision",
  "proposed_fix": "point the anchor at ?tab=decision (or delete it \u2014 the tab is already in the page)",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:35+00:00"
 },
 {
  "id": "AUD-004",
  "title": "sections page: 'View TCs' links to /test-cases, retired with v1",
  "class": "dead link on a live page (link retired, anchor kept)",
  "severity": "low",
  "pass": 1,
  "surface": "/sections \u2014 templates/sections/list.html:46 (shown to admin/tester/superadmin)",
  "reproduction": "GET /sections as a member; click 'View TCs' on any section",
  "evidence": "href=\"/test-cases?section_id={{ node.id }}\"; url_map contains no /test-cases rule (v1 product tables dropped in migration 053, D-191..D-221)",
  "expected": "no link to a retired surface",
  "observed": "404",
  "proposed_fix": "remove the anchor or point it at the claim library filtered by section",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:35+00:00"
 },
 {
  "id": "AUD-005",
  "title": "six templates are rendered by nothing; one of them is documented in CLAUDE.md as a live component and its JS calls a retired v1 API",
  "class": "documentation asserts a component the code no longer wires",
  "severity": "low",
  "pass": 1,
  "surface": "primeqa/templates: claims/inbox.html, claims/list.html, components/_legacy_banner.html, components/feedback_modal.html, dashboard_release.html, run/index.html; static/js/tc_feedback.js",
  "reproduction": "diff <(find primeqa/templates -name '*.html' | sed 's|primeqa/templates/||' | sort) <(grep -rhoE \"render_template\\(\\s*['\\\"][^'\\\"]+|\\{%\\s*(include|extends|import|from)\\s+['\\\"][^'\\\"]+\" primeqa/ | grep -oE \"[a-z_/.]+\\.html\" | sort -u)",
  "evidence": "zero render_template/include/extends references to the six; no dynamic render_template calls exist; CLAUDE.md 'UI component kit' lists `feedback_modal.html` + `static/js/tc_feedback.js` as the thumbs-feedback component; tc_feedback.js:31 calls /api/test-cases/${tcId}/feedback, a route absent from url_map",
  "expected": "CLAUDE.md describes components that exist in the running product",
  "observed": "a documented component is dead code calling a dead endpoint",
  "proposed_fix": "delete the six templates and tc_feedback.js; correct the CLAUDE.md component kit list",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:35+00:00"
 },
 {
  "id": "AUD-006",
  "title": "the public shared-dashboard link and its mint endpoint 500 on every call: `_hash_share_token` was deleted in the v1 retirement while both callers stayed",
  "class": "retirement removed a symbol whose callers survived (same cause as the plan_id-never-selected and dead-link findings)",
  "severity": "high",
  "pass": 1,
  "surface": "GET /shared/<token> (views.py:585) and POST /api/dashboard/share (views.py:475)",
  "reproduction": "GET /shared/anything \u2192 500; grep -rn 'def _hash_share_token' primeqa/ \u2192 0 definitions; git log -S 'def _hash_share_token' \u2192 removed in 7fd518f (D-221.4)",
  "evidence": "render sweep: /shared/no-such-token \u2192 NameError: name '_hash_share_token' is not defined, for every session; the route is unauthenticated by design (the token is the capability), so every share link ever issued now 500s, and no new one can be minted",
  "expected": "a bad token renders the not_found state (404); a good token renders the read-only dashboard; the mint returns a URL",
  "observed": "NameError \u2192 the themed 500 page, on both routes, since the retirement commit",
  "proposed_fix": "either restore a hashing helper (sha256, as the poll-token one) or retire both routes and the shared_dashboard_links table with them; add a guard that every name referenced in views.py resolves (pyflakes F821 as a gate test)",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:04:08+00:00"
 },
 {
  "id": "AUD-007",
  "title": "landing page 500s for a tenant with no runs: the template compares a None pass rate to an integer",
  "class": "a data shape nobody planned: the zero-state of an aggregate",
  "severity": "medium",
  "pass": 1,
  "surface": "GET / \u2192 templates/dashboard.html:50 (`stats.pass_rate >= 80`)",
  "reproduction": "log in as a member of a tenant with zero execution runs (scratch tenant 2); GET /",
  "evidence": "render sweep, member_t2: GET / \u2192 TypeError: '>=' not supported between instances of 'NoneType' and 'int' at dashboard.html:50; the stats builder yields pass_rate=None when there are no runs",
  "expected": "a tenant with no runs sees a landing page with an empty pass-rate cell",
  "observed": "the themed 500 page; a brand-new tenant's first screen is an error",
  "proposed_fix": "coerce pass_rate to 0 (or render an empty state) in the stats builder, and render the legacy dashboard over a zero-run fixture in a page test",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:04:08+00:00"
 },
 {
  "id": "AUD-008",
  "title": "requirement page: the readiness handler commented 'never breaks the page' raises NameError itself \u2014 `logging` is not imported in views.py",
  "class": "self-reporting control: a safety net whose own body cannot run (the fifth instance)",
  "severity": "high",
  "pass": 1,
  "surface": "GET /requirements/<id> \u2014 views.py:2982-2983 (`except Exception: logging.getLogger(...)`), inside requirements_detail",
  "reproduction": "pyflakes primeqa/views.py \u2192 'views.py:2983 undefined name logging'; grep -c '^import logging' primeqa/views.py \u2192 0; make readiness_for_pairs raise (substrate unreachable, a claim with no coverage rows in an odd shape) and GET the requirement page",
  "evidence": "the try/except at views.py:2970-2984 exists so that a readiness failure degrades to an empty readiness map; its except clause calls logging.getLogger, and `logging` is undefined in the module, so the handler raises NameError and the page returns the themed 500 \u2014 exactly when the net was meant to hold",
  "expected": "a readiness read failure logs a warning and the requirement page renders without readiness cells",
  "observed": "a readiness read failure becomes a 500; every day the handler is not exercised it reads as working \u2014 this is a control that reports its own correctness in a comment",
  "proposed_fix": "import logging at module level (views.py uses `log`/`logger` elsewhere \u2014 use the module logger); add pyflakes F821 as a gate test so an undefined name in a handler cannot ship; add a page test that forces readiness_for_pairs to raise and asserts 200",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:07:24+00:00"
 },
 {
  "id": "AUD-009",
  "title": "Jira ticket search 500s whenever an environment or connection is chosen: `_jira_client` / `_jira_client_for_env` are undefined",
  "class": "retirement removed a symbol whose callers survived (same cause as the plan_id-never-selected and dead-link findings)",
  "severity": "high",
  "pass": 1,
  "surface": "GET /api/jira/search \u2014 primeqa/execution/routes.py:65,70 (the survivor of the v1 execution module; execution_bp is registered at app.py:69)",
  "reproduction": "as any member: GET /api/jira/search?q=abc&environment_id=4 \u2192 500 {SERVER_ERROR}; log: NameError: name '_jira_client' is not defined. Without an env/conn the route short-circuits to a hint and the defect hides",
  "evidence": "pyflakes: execution/routes.py:65 undefined name '_jira_client', :70 '_jira_client_for_env'; the render sweep's plain GET returned 200 with the 'pick an environment' hint, which is why a 200-only sweep would never see it",
  "expected": "a Jira search against a configured connection returns matching tickets for the requirement picker",
  "observed": "any real search dies before reaching Jira; the picker that depends on it cannot work",
  "proposed_fix": "restore the two helpers (they lived in the retired v1 metadata/jira module) on the runs Jira client, or retire the route and the picker together; the F821 gate closes the class",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:07:24+00:00"
 },
 {
  "id": "AUD-010",
  "title": "legacy metadata service references `prev_version` before assignment (unreachable today)",
  "class": "retirement removed a symbol whose callers survived (same cause as the plan_id-never-selected and dead-link findings)",
  "severity": "low",
  "pass": 1,
  "surface": "primeqa/metadata/service.py:955 refresh_metadata (v1 metadata module, superseded by semantic/ + sync/)",
  "reproduction": "pyflakes primeqa/metadata/service.py; grep -rn 'refresh_metadata(' primeqa/ \u2192 no callers",
  "evidence": "three undefined-name hits on `prev_version`; zero live callers of refresh_metadata",
  "expected": "dead code is deleted, not kept compiling by accident",
  "observed": "a module CLAUDE.md marks 'superseded' still ships a function that cannot run",
  "proposed_fix": "delete refresh_metadata (and the rest of the v1 metadata service if nothing else imports it)",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:07:24+00:00"
 },
 {
  "id": "AUD-011",
  "title": "release create/update/add-requirement 500 on oversize or wrong-type input: the database is the only validator",
  "class": "no validation at the boundary \u2014 the database column is the validator and it answers 500",
  "severity": "medium",
  "pass": 1,
  "surface": "POST /api/releases, PATCH /api/releases/<id>, POST /releases (HTML form), POST /api/releases/<id>/requirements; PATCH /api/requirements/<id> (found on the hostile world with the same 20k-char body)",
  "reproduction": "as a member: POST /api/releases {\"name\": \"A\"*20000} \u2192 500; PATCH /api/releases/58 {\"name\": \"A\"*20000} \u2192 500; POST /api/releases/58/requirements {\"requirement_id\": \"x\"} \u2192 500; the HTML /releases form with a 20k name \u2192 the themed 500 page",
  "evidence": "scripts/audit/post_all.py on scratch: 4 of 650 hostile POSTs 500; psycopg2 StringDataRightTruncation 'value too long for type character varying(255)' \u00d73 and InvalidTextRepresentation 'invalid input syntax for type integer: \"x\"' \u00d71; stacks end in release/repository.py:27 and :80 and release/service.py:125 with no length or type check on the way; hostile-world POST sweep: PATCH /api/requirements/<id> maximal \u2192 500 StringDataRightTruncation",
  "expected": "400 with a message naming the field; nothing reaches the database",
  "observed": "an unhandled DataError; the server log carries a stack for every oversize submission; the user sees 'Something went wrong'",
  "proposed_fix": "validate name/description length and integer ids in ReleaseService (create_section already does this for sections \u2014 copy that shape); a boundary test that POSTs 20k-char and wrong-type bodies to every create/update route and asserts 4xx",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:24:06+00:00"
 },
 {
  "id": "AUD-012",
  "title": "any member can mint, rotate or revoke ANY release's public status-poll token; CLAUDE.md says Release-Owner or admin",
  "class": "documentation claims a gate the route does not enforce (self-reporting control)",
  "severity": "medium",
  "pass": 3,
  "surface": "POST and DELETE /api/releases/<id>/status-token \u2014 release/routes.py:341-378, gated @require_role('admin','tester') = MEMBER floor, no owner check in the body",
  "reproduction": "as scratch member user 14 (not the creator of release 58): POST /api/releases/58/status-token \u2192 201 with a fresh token; POST again \u2192 201 (rotated: the previous token, held by CI, is now dead); DELETE \u2192 200 revoked",
  "evidence": "post_all.py: four 201s and four 200s for member_t1 on these routes; the body reads only get_release(release_id, tenant_id) then writes status_poll_token_hash; CLAUDE.md 'Public release-status endpoint is token-gated \u2026 Mint + revoke are Release-Owner/admin, tenant-scoped'",
  "expected": "only the release owner or an admin can expose a release's decision status to an unauthenticated poller, or invalidate the token CI already holds",
  "observed": "every member of the tenant can do both, for every release; the act is logged but not gated",
  "proposed_fix": "gate at Tier.ADMIN or add the owner check (release.created_by == request.user['id']); correct CLAUDE.md or the code so they agree; add the route to the authz inventory's expected-tier table",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:24:06+00:00"
 },
 {
  "id": "AUD-013",
  "title": "PASS 4: an execution can be enqueued with NO plan \u2014 the 'every run starts from a RECORDED plan' invariant (D-486) is not enforced at the enqueue chokepoint",
  "class": "an invariant enforced at some entry points and asserted for all of them (self-reporting control)",
  "severity": "critical",
  "pass": 4,
  "surface": "POST /api/s4-execution-jobs (views.py:4535 \u2192 execution_engine/intake.py:enqueue_s4_execution, plan_id Optional=None); the same intake serves POST /claims/<id>/run and /claims/<id>/run-async",
  "reproduction": "on scratch, as an admin (tenant 1): plant an approved claim with an active metadata recipe; POST /api/s4-execution-jobs {test_id, environment_id: 4} with no plan \u2192 202 {job_id: 131, status: queued}; SELECT plan_id FROM s4_execution_jobs WHERE id=131 \u2192 NULL. Two earlier attempts were refused for OTHER reasons (environment scope; no executable recipe) \u2014 none of the refusals was 'no plan'",
  "evidence": "job 131 queued on scratch with plan_id NULL, created_by 15; intake.enqueue_s4_execution signature `plan_id: Optional[str] = None`; the route's own docstring names a production-confirm gate and an admin gate and no plan gate; D-486 records 'every execution starts from a RECORDED plan \u2026 /run retired', and the memory ledger already counts 568 plan-less stamped runs since the re-enable as evidence",
  "expected": "a loud refusal naming the missing plan, at the ONE execution chokepoint, for every entry point",
  "observed": "the release, requirement and schedule entry points plan first; the API and the two claim-level routes hand the intake a job with no plan and it accepts it \u2014 the invariant holds by convention of the caller, not by the chokepoint",
  "proposed_fix": "make plan_id REQUIRED in enqueue_s4_execution and refuse without it (PlanRefused), then either point the claim-level routes and the API at the planner (plan-for-one-claim) or retire them as /run was; add a guard test that enqueues without a plan through every route and asserts the refusal. The 568 plan-less runs already on production are the evidence this path is exercised",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:28:35+00:00"
 },
 {
  "id": "AUD-014",
  "title": "PASS 4 #2: Evaluate WRITES a recorded GO decision over zero checks for a release whose only declared target is an inactive environment \u2014 while the release board says it will refuse",
  "class": "a list must not contradict the page it links to (D-491), and a gate must not read GO over nothing",
  "severity": "critical",
  "pass": 4,
  "surface": "/releases (board card) vs /releases/<id>?tab=decision (quality decision card) \u2014 hostile release AUD-release-inactive-env: one requirement (79 approved claims), one target: an environment with is_active=false",
  "reproduction": "plant a release with one requirement of 79 approved claims and declare a single target environment that is inactive (scripts/audit/hostile_world.py); GET /releases \u2192 the card reads 'Evaluate will refuse \u2014 79 items in scope are not current \u00b7 1 requirement \u00b7 0 of 79 functional checks current'; GET /releases/<id>?tab=decision \u2192 data-recommendation=\"go\", 'targets (declared): AUD-inactive: 0 of 0 checks current', 'no rule triggered', 'Grading Nothing ungraded'",
  "evidence": "counts.py + the page fragments: the board counts the 79 claims as in-scope-and-not-current; the decision preview scopes to the declared (inactive) target, finds 0 checks, and with nothing to grade recommends GO; the recorded decision row (planted) reads CANNOT DETERMINE beside it; PASS 4 attack #2 on scratch, as admin: POST /releases/<id>/evaluate-decision \u2192 a new release_decisions row, recommendation=go, on the inactive-only release (decisions 13 \u2192 14); the preview had already read GO over '0 of 0 checks current'; PASS 4 attack #9 on the NO-SCOPE release (zero requirements, zero claims): Evaluate was not refused \u2014 it recorded decision 32, recommendation=go, criteria_met every axis true, policy Plimsol default v1, plan NULL. The inactive-only release recorded decision 33 the same way. Both are the same defect: the policy grades an EMPTY input as satisfied on all six axes",
  "expected": "one answer: an inactive-only target is either refused (no active environment in scope \u2014 the D-485 interim) on BOTH surfaces, or evaluated on both; and a preview with zero checks never reads GO",
  "observed": "the list says refuse, the page previews GO, and Evaluate RECORDS GO \u2014 a release can be recommended and recorded GO by deactivating its only target environment after declaring it; nothing was checked",
  "proposed_fix": "Evaluate must REFUSE (cannot_determine, no row) when the resolved scope holds zero checks or zero active environments \u2014 D9's 'ungraded inputs block GO' applied to an empty input, and D-484's 'Evaluate refuses non-current scope' applied to no scope at all; the board and the tab call the same scope filter (D-491); page tests over a no-scope release and an inactive-only target",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:40+00:00"
 },
 {
  "id": "AUD-015",
  "title": "an expired waiver vanishes from the Waivers page, which then reads 'no waivers'",
  "class": "a data shape nobody planned: the aged-out row is hidden rather than shown expired",
  "severity": "medium",
  "pass": 2,
  "surface": "/settings/waivers \u2014 with the tenant's only waiver expired (planted: expires_at = now \u2212 1 day, created 3 days ago)",
  "reproduction": "insert a waiver whose expires_at is past (the service refuses a past expiry, so by direct insert \u2014 which is exactly what time does to a live one); GET /settings/waivers",
  "evidence": "the page renders data-testid=\"no-waivers\" and nothing else; the waiver's reason and item are absent; no 'expired' count",
  "expected": "the waiver listed as expired (its item is no longer accepted, and a human should see that the acceptance lapsed and by whom it was given), or at least 'N expired' beside the active list",
  "observed": "the acceptance and its lapse are invisible; the page tells a reviewer there were never any waivers",
  "proposed_fix": "list expired waivers in their own section with the expiry date; the release decision tab's 'N pending, N waived by <actor> until <date>' line already has the vocabulary",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:40+00:00"
 },
 {
  "id": "AUD-016",
  "title": "the conformance run page answers 200 'the report store is unavailable right now' for a run id that does not exist \u2014 and logs a uuid cast error for a non-uuid id",
  "class": "a lookup miss reported as an infrastructure failure",
  "severity": "medium",
  "pass": 2,
  "surface": "GET /runs/conformance/<job_id> (the parameter is untyped)",
  "reproduction": "GET /runs/conformance/abc, /runs/conformance/161, /runs/conformance/00000000-0000-4000-8000-000000000000 as a member",
  "evidence": "all three \u2192 200 with 'UI conformance \u00b7 Run abc\u2026 \u00b7 Verdict listing \u00b7 The report store is unavailable right now'; the log carries psycopg2 InvalidTextRepresentation invalid input syntax for type uuid: \"161\" / \"abc\" from the lookup",
  "expected": "404 for an id that names nothing; the store's unavailability said only when the store is unavailable",
  "observed": "a bad link reads as an outage; the cast error is swallowed into the same message, so the log and the page disagree about what happened",
  "proposed_fix": "type the parameter <uuid:job_id> so a non-uuid is a 404 at the router; 404 on a miss; keep the 'store unavailable' sentence for a store error only",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:40+00:00"
 },
 {
  "id": "AUD-017",
  "title": "an unstamped run made today reads 'this run predates run-level environment stamping' \u2014 the sentence assumes the run is legacy",
  "class": "a status sentence that explains one cause as if it were the only cause",
  "severity": "low",
  "pass": 2,
  "surface": "/runs/<run_id> and the requirement page's readiness pill, for a run with org_version_seq NULL created after Step 2 shipped",
  "reproduction": "insert an s4_execution_runs row with a NULL stamp dated today (the hostile world does); open the run",
  "evidence": "pill title: 'Freshness unknown \u2014 this run predates run-level environment stamping. Run again to establish current readiness.' on a run from 5 minutes ago",
  "expected": "the CANNOT_DETERMINE / unstamped state named without asserting when or why the stamp is missing",
  "observed": "a run that lost its stamp for any other reason is described as pre-dating the feature",
  "proposed_fix": "say 'this run carries no environment stamp' and, if the run predates Step 2's deploy, add 'it predates run-level stamping'; the date is on the row",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:41+00:00"
 },
 {
  "id": "AUD-018",
  "title": "`claim_sets.status = 'revoked'` is read by the approval path and written by nothing",
  "class": "a state the reader handles that no producer emits",
  "severity": "low",
  "pass": 2,
  "surface": "primeqa/test_representation/claim_sets.py:223 (approve refuses a 'revoked' set); no code path sets the status; only members are revoked (revoke_member)",
  "reproduction": "grep -n \"status = 'revoked'\\|'revoked'\" primeqa/test_representation/claim_sets.py \u2014 one read, zero writes; the hostile world planted the state by direct insert and /settings/claim-sets rendered it 'revoked' correctly",
  "evidence": "the reader coped (the page shows the cloned set as revoked, 0 of 152 members), so this is not a defect on a page; it is a vocabulary the product speaks but cannot produce",
  "expected": "either a service that revokes a whole set (audited, with a reason) or the dead branch removed",
  "observed": "a status that can only arrive by hand-editing the table",
  "proposed_fix": "add revoke_claim_set (all members + the set, one audited act) or delete the branch; either way, a test that every status value read has a writer",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:41+00:00"
 },
 {
  "id": "AUD-019",
  "title": "quality-policy activation is gated at MEMBER, below the Settings surface that hosts it",
  "class": "capability enforced at the route below the audience the surface declares",
  "severity": "medium",
  "pass": 3,
  "surface": "POST /settings/quality-policy/activate \u2014 views.py:5074 @require_tier(Tier.MEMBER); the Settings nav item is shown only when can_see_settings (superadmin or a manage_* capability); D-488: 'human activation (audited, CLI or Settings) before any policy grades' \u2014 the control AK had to reach for a CLI to use",
  "reproduction": "as a tester (MEMBER): POST /settings/quality-policy/activate with policy_id=<a draft> \u2014 the route admits the tier; on the hostile world the member's five garbage POSTs reached the route (302 back to the page) and only failed to activate because none named a draft",
  "evidence": "post_all.py member_t1 \u2192 302 /settings/quality-policy for every variant (not the '/' deny redirect); the route decorator reads Tier.MEMBER; the page that offers the button is behind the admin-only Settings nav",
  "expected": "the act that decides what grades every release sits at the same tier as the surface that offers it (ADMIN), or the audience is stated and tested",
  "observed": "a member who types the URL can activate \u2014 and thereby retire the active policy (activate retires the previous active version in the same act)",
  "proposed_fix": "gate at Tier.ADMIN (matching /settings/*), and add the route to the authz expected-tier table so the gap cannot reopen",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:06:51+00:00"
 },
 {
  "id": "AUD-020",
  "title": "a member revoked a waiver another user recorded, with an EMPTY revocation reason",
  "class": "an act with no owner check and no reason check on the undo side",
  "severity": "medium",
  "pass": 3,
  "surface": "POST /settings/waivers/<id>/revoke and /releases/<id>/waivers/<id>/revoke \u2014 the planted waiver (created_by 15, reviewer 15) revoked by user 14 with reason ''",
  "reproduction": "as member 14: POST /settings/waivers/<planted>/revoke with an empty form \u2192 302 to the waivers page; SELECT revoked_by, revocation_reason FROM quality_waivers \u2192 14, ''",
  "evidence": "scratch state after the hostile POST sweep: revoked_by=14 reason='' on a waiver whose creator and reviewer are user 15; record_waiver refuses a missing reason on creation ('a waiver carries its reason') \u2014 the undo does not",
  "expected": "revocation by the reviewer, the creator or an admin, with a reason recorded, since a waiver is 'a named human accepting a known state' (D-488) and its withdrawal is the same kind of act",
  "observed": "any member can silently withdraw any acceptance in the tenant, and the ledger shows who but not why",
  "proposed_fix": "require a non-empty revocation_reason (the column exists) and an owner-or-admin check in revoke_waiver; a test that a member's empty revoke is refused",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:06:51+00:00"
 },
 {
  "id": "AUD-021",
  "title": "planning accepts an empty form and records an immutable plan row \u2014 twelve garbage plans from one member's five-body sweep",
  "class": "no validation at the boundary \u2014 an immutable ledger row from no input",
  "severity": "low",
  "pass": 1,
  "surface": "POST /plans (tenant-wide), POST /releases/<id>/plan, POST /requirements/<id>/plan \u2014 each recorded a run_plans row for the empty, maximal, wrong-type and missing-required bodies",
  "reproduction": "as a member: POST /plans with an empty body \u2192 302 to the new plan; SELECT count(*) FROM run_plans WHERE planned_by = <member> \u2192 grows by one per POST",
  "evidence": "12 run_plans rows planned_by 14 after the hostile POST sweep; run_plans are never deleted by design (D-486), so every garbage POST is a permanent row",
  "expected": "a plan needs a stated environment (or the resolved default named back) and the empty form is refused; a missing environment is one of the planner's own refusal reasons",
  "observed": "the tenant-wide and per-scope plan forms record on nothing; the ledger fills with plans nobody asked for",
  "proposed_fix": "require environment_id at the route (400 / flash) before planner.plan; keep the planner's own refusals for the resolved shape",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:06:51+00:00"
 },
 {
  "id": "AUD-022",
  "title": "a requirement whose every claim is deprecated shows no trace of them: 'no functional check', no chip, no count",
  "class": "a data shape nobody planned: the fully-retired scope reads as never-tested",
  "severity": "low",
  "pass": 2,
  "surface": "/requirements (board row) and /requirements/<id> for the hostile requirement AUD-DEP (five claims, all deprecated)",
  "reproduction": "plant a requirement with five approved claims and deprecate all five (scripts/audit/hostile_world.py); GET /requirements \u2192 the row reads 'functional: no functional check', no approved/draft chip; GET the requirement \u2192 zero claim rows, no 'N deprecated'",
  "evidence": "board_rows excludes status='deprecated' by design (D-219: retired from the corpus) and the page lists live claims only; nothing says five existed",
  "expected": "the retirement is visible: 'no live test case \u00b7 5 deprecated' \u2014 a reviewer can tell never-tested from all-retired",
  "observed": "both read as never-tested",
  "proposed_fix": "carry a deprecated count into the board row and the page header (the D-219 exclusion stays for grading)",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:06:52+00:00"
 },
 {
  "id": "AUD-023",
  "title": "a member deactivated a release target another user declared \u2014 the undo of a declared act carries no owner check",
  "class": "an act with no owner check and no reason check on the undo side",
  "severity": "medium",
  "pass": 3,
  "surface": "POST /releases/<id>/targets/<env>/remove \u2014 the hostile release's only target (declared_by 15) removed by member 14 with a valid body",
  "reproduction": "as member 14: POST /releases/<id>/targets/<env>/remove {reason} \u2192 302; SELECT active, deactivated_by FROM release_targets \u2192 false, 14",
  "evidence": "scratch state after the probe: active=false deactivated_by=14 on a target declared by 15; the same member could not finalise 15's decision (refused), so the two undo paths differ",
  "expected": "a declared target is removed by its declarer or an admin (D-486 records the actor on the declaration for exactly this), or the audience is stated",
  "observed": "any member can reshape another user's release scope, and the release then reads as if the declarer had changed their mind",
  "proposed_fix": "owner-or-admin on target removal (declared_by == user or ADMIN); a test that a member's removal of another user's target is refused",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:07:58+00:00"
 },
 {
  "id": "AUD-024",
  "title": "a claim, run or job id that names nothing renders a 200 'not found' page instead of a 404",
  "class": "a lookup miss reported as a page (same cause as AUD-016)",
  "severity": "low",
  "pass": 3,
  "surface": "/claims/<uuid>, /claims/<uuid>/panel, /claims/<uuid>/run-status/<job>, /runs/<uuid>, /runs/conformance/<id> \u2014 observed from a tenant-2 member asking for tenant-1 ids (the isolation sweep), which is the same as any bad link",
  "reproduction": "as any member: GET /claims/<a uuid that exists nowhere in your tenant> \u2192 200 'Test case not found \u2014 it may have been superseded or removed'; GET /runs/<such a uuid> \u2192 200 'Test run' shell",
  "evidence": "scripts/audit/isolation.py: 108 cross-tenant requests, zero tenant-1 markers in any body, zero 2xx acts \u2014 isolation HOLDS; eight 200s were all not-found shells or empty lists",
  "expected": "404 for a miss, so monitors, crawlers and CI tell a dead link from a page",
  "observed": "a 200 that says 'not found' in prose",
  "proposed_fix": "return 404 with the same template (the shared-dashboard route already does this for a bad token)",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:09:40+00:00"
 },
 {
  "id": "AUD-025",
  "title": "orphan routes: the sections page and the tenant-wide plan form are reachable but nothing links to them; five retired aliases redirect without a link; 71 of 78 API routes have no UI caller",
  "class": "reachable, unlinked \u2014 a surface nobody can find is a surface nobody tests",
  "severity": "low",
  "pass": 1,
  "surface": "/sections (GET, a live page), POST /plans (the tenant-wide plan), POST /requirements/<id>/run-substrate (already refusing), and the bookmark redirects /results, /results/<id>, /suites/<id>, /tickets; two more (/claims/<id>/panel, /environments/<id>/sync-substrate/status) are reached by JS-built URLs and are not orphans",
  "reproduction": "scripts/audit/deadlinks.py's target set (every href/action/hx-*/data-*/fetch literal in templates and static JS) matched against the live url_map; a page-facing rule with no matching target is listed",
  "evidence": "128 page-facing rules, 9 with no static target; 78 /api rules, 71 never named by a template or static JS \u2014 the API is a programmatic surface (CI, CLI, tests), so those are coverage facts, not defects",
  "expected": "every page either has a way in or is retired; a route kept only for bookmarks says so in code",
  "observed": "the sections page is live and unreachable from the nav; the tenant-wide plan can only be recorded by hand-crafting a POST (and, per AUD-021, from an empty one)",
  "proposed_fix": "link /sections from Settings or retire it; retire POST /plans or give it a form; keep the five aliases but mark them as redirect-only in the inventory",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:10:36+00:00"
 },
 {
  "id": "AUD-026",
  "title": "PASS 4 #1: a PASS verdict for a run that does not exist is insertable \u2014 s6_interpretations.run_id has no foreign key to s4_execution_runs",
  "class": "an invariant the ledger states that the schema does not hold (the readers happen to join through runs)",
  "severity": "medium",
  "pass": 4,
  "surface": "tenant schema: s6_interpretations (verdicts) \u2192 s4_execution_runs (runs); pg_constraint lists NO foreign key on run_id",
  "reproduction": "INSERT INTO s6_interpretations (run_id, recipe_id, claim_test_id, outcome, verdict, detail) VALUES (<random uuid>, <random uuid>, <a claim>, 'passed', 'passed', '{}') \u2192 accepted; SELECT count(*) FROM s6_interpretations i WHERE NOT EXISTS (SELECT 1 FROM s4_execution_runs r WHERE r.run_id = i.run_id) \u2192 1",
  "evidence": "D-466 records that PASS requires positive attestation over captured evidence; that holds in the interpreter's writer, not in the table. The decision path reads verdicts through a join on s4_execution_runs (_COUNTED_RUNS_BULK_SQL), so the orphan is invisible to grading today \u2014 the invariant is held by every reader's join, not by the schema",
  "expected": "a verdict cannot exist without the run it interprets: a foreign key, and a guard that no verdict row is orphaned",
  "observed": "a PASS with no run behind it sits in the table; any reader that lists verdicts without the join would count it",
  "proposed_fix": "add the FK (ON DELETE CASCADE with the run's own retention), verify zero orphans on production before applying, and add a schema test that every verdict's run exists",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:14:58+00:00"
 }
]