[
 {
  "id": "AUD-001",
  "title": "authz oracle misreports three authenticated routes as open: it does not resolve the `_require_auth_api` alias",
  "class": "self-reporting control: the gate oracle reads decorator NAMES, not the decorators that run",
  "severity": "medium",
  "pass": 1,
  "surface": "scripts/authz_inventory.py (the D-245 regression oracle)",
  "reproduction": "python scripts/authz_inventory.py | grep '(none'",
  "evidence": "/api/s3-generation-jobs/<id>, /api/s4-execution-jobs/<id>, /api/s3-generation-jobs/<id>/cancel listed as '(none — auth/login only or open)'; views.py:245 `from primeqa.core.auth import require_auth as _require_auth_api`; the three routes carry @_require_auth_api",
  "expected": "the oracle names the gate that actually runs on every route",
  "observed": "any alias of a gate is invisible to it; the oracle can be green while a route is open, or red while it is gated — its report is not evidence of either",
  "proposed_fix": "derive gates from the live app (walk __wrapped__ / closure args) or resolve import aliases in the AST; add a guard asserting the oracle and the live map agree",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:34+00:00",
  "closed_at": "2026-09-21T10:45:54+00:00",
  "closed_by": "round3 part B (housekeeping), 2026-09-21",
  "closure": "scripts/authz_inventory.py rebuilt: it imports the LIVE app and derives every gate by walking each endpoint's __wrapped__ chain AND its closure cells, recognising a gate by the CODE OBJECT its decorator produces — never by a name, which functools.wraps copies and an alias hides. The Tier is read from the closure cell that holds it; rows carry {endpoint, methods, rule, file:line, gates, min_tier}. The three aliased routes now report require_auth. GUARD tests/unit/test_authz_oracle.py (5): the oracle and the live url_map agree row for row and gate for gate; the three aliased routes report their gate (RED on main: '(none — auth/login only or open)'); the set of routes with NO gate is exactly the nine named anonymous ones, so a route that drops to open fails the day it does; a planted app proves an aliased gate is read, a tiered gate reports its Tier, and an open route is reported open"
 },
 {
  "id": "AUD-002",
  "title": "the template dead-link sweep silently excludes every href that carries a Jinja expression",
  "class": "a sweep that skips what it cannot parse reports the residue as clean",
  "severity": "medium",
  "pass": 1,
  "surface": "tests/unit/test_step_6a_routes.py::test_no_template_links_to_a_path_that_does_not_answer",
  "reproduction": "sed -n 63p tests/unit/test_step_6a_routes.py — href regex is /[^\"{}\\s]*/, so any href containing `{` is never checked; scripts/audit/deadlinks.py url_map.json finds what it skipped",
  "evidence": "166 internal targets in templates+JS; the unit sweep can see at most the 60 with no Jinja; 5 dead targets found, 2 on live pages (AUD-003, AUD-004); the sweep also ignores action=, hx-*, data-confirm-form and fetch() literals",
  "expected": "a dead-link sweep resolves every target a browser or htmx will follow, parameterised or not",
  "observed": "the sweep has been green through D-490, D-491 and the retirement while two dead links sat on live pages",
  "proposed_fix": "replace the sweep's regex with scripts/audit/deadlinks.py's resolver (all attributes, Jinja collapsed to a wildcard segment, if-branches expanded) and gate on zero dead",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:35+00:00",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @4da6e32",
  "closure": "the sweep replaced by scripts/deadlinks_gate.py (every attribute, htmx verb, form action, url_for with arguments, JS literals; Jinja collapsed to wildcard segments; the method checked) and proven able to fail in tests/unit/test_no_dead_links.py before the repo is judged; repo-wide 283 targets / 4 dead before, 277 / 0 dead after; in the pre-commit hook beside pyflakes"
 },
 {
  "id": "AUD-003",
  "title": "release detail: 'View Reasoning' links to /releases/<id>/decision, a route that does not exist",
  "class": "dead link on a live page (link retired, anchor kept)",
  "severity": "medium",
  "pass": 1,
  "surface": "/releases/<id> — templates/releases/detail.html:74, inside the latest-decision block",
  "reproduction": "open any release that holds a decision (production release 16 holds decision 72); click 'View Reasoning →'",
  "evidence": "templates/releases/detail.html:74 href=\"/releases/{{ release.id }}/decision\"; url_map has no /releases/<int>/decision rule (only evaluate-decision, plan, run, targets, waivers); the decision lives at ?tab=decision",
  "expected": "the link opens the decision tab",
  "observed": "404 on the programme's most-viewed page, for exactly the releases that have a decision",
  "proposed_fix": "point the anchor at ?tab=decision (or delete it — the tab is already in the page)",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:35+00:00",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @4da6e32",
  "closure": "View Reasoning -> /releases/<id>?tab=decision"
 },
 {
  "id": "AUD-004",
  "title": "sections page: 'View TCs' links to /test-cases, retired with v1",
  "class": "dead link on a live page (link retired, anchor kept)",
  "severity": "low",
  "pass": 1,
  "surface": "/sections — templates/sections/list.html:46 (shown to admin/tester/superadmin)",
  "reproduction": "GET /sections as a member; click 'View TCs' on any section",
  "evidence": "href=\"/test-cases?section_id={{ node.id }}\"; url_map contains no /test-cases rule (v1 product tables dropped in migration 053, D-191..D-221)",
  "expected": "no link to a retired surface",
  "observed": "404",
  "proposed_fix": "remove the anchor or point it at the claim library filtered by section",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:35+00:00",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @4da6e32",
  "closure": "View TCs -> View requirements at /requirements?section_id=<id> (the requirements list reads section_id)"
 },
 {
  "id": "AUD-005",
  "title": "six templates are rendered by nothing; one of them is documented in CLAUDE.md as a live component and its JS calls a retired v1 API",
  "class": "documentation asserts a component the code no longer wires",
  "severity": "low",
  "pass": 1,
  "surface": "primeqa/templates: claims/inbox.html, claims/list.html, components/_legacy_banner.html, components/feedback_modal.html, dashboard_release.html, run/index.html; static/js/tc_feedback.js",
  "reproduction": "diff <(find primeqa/templates -name '*.html' | sed 's|primeqa/templates/||' | sort) <(grep -rhoE \"render_template\\(\\s*['\\\"][^'\\\"]+|\\{%\\s*(include|extends|import|from)\\s+['\\\"][^'\\\"]+\" primeqa/ | grep -oE \"[a-z_/.]+\\.html\" | sort -u)",
  "evidence": "zero render_template/include/extends references to the six; no dynamic render_template calls exist; CLAUDE.md 'UI component kit' lists `feedback_modal.html` + `static/js/tc_feedback.js` as the thumbs-feedback component; tc_feedback.js:31 calls /api/test-cases/${tcId}/feedback, a route absent from url_map",
  "expected": "CLAUDE.md describes components that exist in the running product",
  "observed": "a documented component is dead code calling a dead endpoint",
  "proposed_fix": "delete the six templates and tc_feedback.js; correct the CLAUDE.md component kit list",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T06:56:35+00:00",
  "closure": "the four templates still rendered by nothing are deleted (claims/inbox.html, components/_legacy_banner.html, dashboard_release.html, run/index.html); the other two the audit named (claims/list.html, components/feedback_modal.html) and static/js/tc_feedback.js had already gone with an earlier slice, and CLAUDE.md's component kit no longer names them (verified against the file). Also deleted: the dead bulk-generate half of static/js/requirements_list.js — no element it named has existed in the template since D-165 and it called POST /api/requirements/bulk-generate, retired with the v1 model layer (7fd518f); the row delete/restore/purge half is live and stays. GUARD tests/unit/test_documented_contracts.py: every primeqa path CLAUDE.md names exists, and every component the kit lists is rendered or imported by something",
  "closed_at": "2026-09-21T10:45:54+00:00",
  "closed_by": "round3 part B (housekeeping), 2026-09-21"
 },
 {
  "id": "AUD-006",
  "title": "the public shared-dashboard link and its mint endpoint 500 on every call: `_hash_share_token` was deleted in the v1 retirement while both callers stayed",
  "class": "retirement removed a symbol whose callers survived (same cause as the plan_id-never-selected and dead-link findings)",
  "severity": "high",
  "pass": 1,
  "surface": "GET /shared/<token> (views.py:585) and POST /api/dashboard/share (views.py:475)",
  "reproduction": "GET /shared/anything → 500; grep -rn 'def _hash_share_token' primeqa/ → 0 definitions; git log -S 'def _hash_share_token' → removed in 7fd518f (D-221.4)",
  "evidence": "render sweep: /shared/no-such-token → NameError: name '_hash_share_token' is not defined, for every session; the route is unauthenticated by design (the token is the capability), so every share link ever issued now 500s, and no new one can be minted",
  "expected": "a bad token renders the not_found state (404); a good token renders the read-only dashboard; the mint returns a URL",
  "observed": "NameError → the themed 500 page, on both routes, since the retirement commit",
  "proposed_fix": "either restore a hashing helper (sha256, as the poll-token one) or retire both routes and the shared_dashboard_links table with them; add a guard that every name referenced in views.py resolves (pyflakes F821 as a gate test)",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:04:08+00:00",
  "closed_at": "2026-09-19T06:36:04+00:00",
  "closed_by": "D-495 (merge 0b36d91, 2026-09-15)",
  "closure": "D-495 (merge 0b36d91, 2026-09-15): _hash_share_token restored verbatim; the share route proven 404 on production"
 },
 {
  "id": "AUD-007",
  "title": "landing page 500s for a tenant with no runs: the template compares a None pass rate to an integer",
  "class": "a data shape nobody planned: the zero-state of an aggregate",
  "severity": "medium",
  "pass": 1,
  "surface": "GET / → templates/dashboard.html:50 (`stats.pass_rate >= 80`)",
  "reproduction": "log in as a member of a tenant with zero execution runs (scratch tenant 2); GET /",
  "evidence": "render sweep, member_t2: GET / → TypeError: '>=' not supported between instances of 'NoneType' and 'int' at dashboard.html:50; the stats builder yields pass_rate=None when there are no runs",
  "expected": "a tenant with no runs sees a landing page with an empty pass-rate cell",
  "observed": "the themed 500 page; a brand-new tenant's first screen is an error",
  "proposed_fix": "coerce pass_rate to 0 (or render an empty state) in the stats builder, and render the legacy dashboard over a zero-run fixture in a page test",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:04:08+00:00",
  "closed_at": "2026-09-21T09:08:36+00:00",
  "closed_by": "round3 part A (what a pilot client hits first), 2026-09-21",
  "closure": "dashboard.html renders an empty state for the pass-rate card when the rate is None (no run) — '— · no run yet · the rate appears after the first run' — and the number with its colour only when a run exists; proven DB-real (tenant 2, no run: 200 with pass-rate-empty; RED on main: Unhandled TypeError on GET /); fixture screenshot landing_zero_run_tenant.png; production render PRODUCTION_landing.png (94.0%, the numeric branch)"
 },
 {
  "id": "AUD-008",
  "title": "requirement page: the readiness handler commented 'never breaks the page' raises NameError itself — `logging` is not imported in views.py",
  "class": "self-reporting control: a safety net whose own body cannot run (the fifth instance)",
  "severity": "high",
  "pass": 1,
  "surface": "GET /requirements/<id> — views.py:2982-2983 (`except Exception: logging.getLogger(...)`), inside requirements_detail",
  "reproduction": "pyflakes primeqa/views.py → 'views.py:2983 undefined name logging'; grep -c '^import logging' primeqa/views.py → 0; make readiness_for_pairs raise (substrate unreachable, a claim with no coverage rows in an odd shape) and GET the requirement page",
  "evidence": "the try/except at views.py:2970-2984 exists so that a readiness failure degrades to an empty readiness map; its except clause calls logging.getLogger, and `logging` is undefined in the module, so the handler raises NameError and the page returns the themed 500 — exactly when the net was meant to hold",
  "expected": "a readiness read failure logs a warning and the requirement page renders without readiness cells",
  "observed": "a readiness read failure becomes a 500; every day the handler is not exercised it reads as working — this is a control that reports its own correctness in a comment",
  "proposed_fix": "import logging at module level (views.py uses `log`/`logger` elsewhere — use the module logger); add pyflakes F821 as a gate test so an undefined name in a handler cannot ship; add a page test that forces readiness_for_pairs to raise and asserts 200",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:07:24+00:00",
  "closed_at": "2026-09-19T06:36:04+00:00",
  "closed_by": "D-495",
  "closure": "D-495: `import logging` at module level; a page test forces readiness to raise and asserts 200"
 },
 {
  "id": "AUD-009",
  "title": "Jira ticket search 500s whenever an environment or connection is chosen: `_jira_client` / `_jira_client_for_env` are undefined",
  "class": "retirement removed a symbol whose callers survived (same cause as the plan_id-never-selected and dead-link findings)",
  "severity": "high",
  "pass": 1,
  "surface": "GET /api/jira/search — primeqa/execution/routes.py:65,70 (the survivor of the v1 execution module; execution_bp is registered at app.py:69)",
  "reproduction": "as any member: GET /api/jira/search?q=abc&environment_id=4 → 500 {SERVER_ERROR}; log: NameError: name '_jira_client' is not defined. Without an env/conn the route short-circuits to a hint and the defect hides",
  "evidence": "pyflakes: execution/routes.py:65 undefined name '_jira_client', :70 '_jira_client_for_env'; the render sweep's plain GET returned 200 with the 'pick an environment' hint, which is why a 200-only sweep would never see it",
  "expected": "a Jira search against a configured connection returns matching tickets for the requirement picker",
  "observed": "any real search dies before reaching Jira; the picker that depends on it cannot work",
  "proposed_fix": "restore the two helpers (they lived in the retired v1 metadata/jira module) on the runs Jira client, or retire the route and the picker together; the F821 gate closes the class",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:07:24+00:00",
  "closed_at": "2026-09-19T06:36:04+00:00",
  "closed_by": "D-495",
  "closure": "D-495: the Jira search route answers 'not available' instead of raising; the rebuild is a FIX PLAN slice"
 },
 {
  "id": "AUD-010",
  "title": "legacy metadata service references `prev_version` before assignment (unreachable today)",
  "class": "retirement removed a symbol whose callers survived (same cause as the plan_id-never-selected and dead-link findings)",
  "severity": "low",
  "pass": 1,
  "surface": "primeqa/metadata/service.py:955 refresh_metadata (v1 metadata module, superseded by semantic/ + sync/)",
  "reproduction": "pyflakes primeqa/metadata/service.py; grep -rn 'refresh_metadata(' primeqa/ → no callers",
  "evidence": "three undefined-name hits on `prev_version`; zero live callers of refresh_metadata",
  "expected": "dead code is deleted, not kept compiling by accident",
  "observed": "a module CLAUDE.md marks 'superseded' still ships a function that cannot run",
  "proposed_fix": "delete refresh_metadata (and the rest of the v1 metadata service if nothing else imports it)",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:07:24+00:00",
  "closed_at": "2026-09-19T06:36:04+00:00",
  "closed_by": "D-495",
  "closure": "D-495: refresh_metadata (333 lines, zero callers) deleted"
 },
 {
  "id": "AUD-011",
  "title": "release create/update/add-requirement 500 on oversize or wrong-type input: the database is the only validator — RUN 2: seven routes",
  "class": "no validation at the boundary — the database column is the validator and it answers 500",
  "severity": "medium",
  "pass": 1,
  "surface": "POST /api/releases, PATCH /api/releases/<id>, POST /releases (HTML form), POST /api/releases/<id>/requirements; PATCH /api/requirements/<id> (found on the hostile world with the same 20k-char body)",
  "reproduction": "as a member: POST /api/releases {\"name\": \"A\"*20000} → 500; PATCH /api/releases/58 {\"name\": \"A\"*20000} → 500; POST /api/releases/58/requirements {\"requirement_id\": \"x\"} → 500; the HTML /releases form with a 20k name → the themed 500 page",
  "evidence": "scripts/audit/post_all.py on scratch: 4 of 650 hostile POSTs 500; psycopg2 StringDataRightTruncation 'value too long for type character varying(255)' ×3 and InvalidTextRepresentation 'invalid input syntax for type integer: \"x\"' ×1; stacks end in release/repository.py:27 and :80 and release/service.py:125 with no length or type check on the way; hostile-world POST sweep: PATCH /api/requirements/<id> maximal → 500 StringDataRightTruncation | RUN 2 (hostile world v2, 20,000-character fields): 500 StringDataRightTruncation on POST /api/releases, PATCH /api/releases/<id>, POST /releases (the web form), PATCH /api/environments/<id>, PATCH /api/sections/<id>, PATCH /api/requirements/<id>; and 500 InvalidTextRepresentation on POST /api/releases/<id>/requirements with requirement_id 'x' (wrong type)",
  "expected": "400 with a message naming the field; nothing reaches the database",
  "observed": "an unhandled DataError; the server log carries a stack for every oversize submission; the user sees 'Something went wrong'",
  "proposed_fix": "validate name/description length and integer ids in ReleaseService (create_section already does this for sections — copy that shape); a boundary test that POSTs 20k-char and wrong-type bodies to every create/update route and asserts 4xx",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:24:06+00:00",
  "closed_at": "2026-09-21T05:07:36+00:00",
  "closed_by": "triage-round2 batch 2 (boundary validation), 2026-09-21",
  "closure": "primeqa/shared/validation.py — the create_section shape lifted: columns(model, values) checks every value against its COLUMN (String(n) length, integer range, boolean, date, JSON shape, NUL-free) BEFORE any row is built, refusing with a ValueError that names the field (→ 400); applied in ReleaseService (create/update/add_requirement), TestManagementService (create/update requirement, update section, import-jira), GroupService (create/add member/add environment), EnvironmentService (create/update/store_credentials), ConnectionService (create/update); the web release and connection forms re-render with the reason as a 400, the group and requirement-edit forms flash it (the requirement edit now goes through the service, not the repo). THE GUARD tests/integration/test_boundary_gate.py: generated from the url_map, every body-reading write route (69) x {empty, maximal 20k, wrong-type incl. NUL} as the table's minimum tier — RED on the pre-change tree with 24 route x body rows at 500 across 15 routes (the audit's seven plus /api/groups, /api/groups/<id>/environments|members, /api/environments/<id>/credentials, /api/requirements, /api/requirements/import-jira, /groups, /connections/<id>/edit, /requirements/<id>/edit) — GREEN now: 207 requests, 0 at 500, every /api/* answer to an oversize or mistyped body a 4xx, residue 0"
 },
 {
  "id": "AUD-012",
  "title": "any member can mint, rotate or revoke ANY release's public status-poll token; CLAUDE.md says Release-Owner or admin",
  "class": "documentation claims a gate the route does not enforce (self-reporting control)",
  "severity": "medium",
  "pass": 3,
  "surface": "POST and DELETE /api/releases/<id>/status-token — release/routes.py:341-378, gated @require_role('admin','tester') = MEMBER floor, no owner check in the body",
  "reproduction": "as scratch member user 14 (not the creator of release 58): POST /api/releases/58/status-token → 201 with a fresh token; POST again → 201 (rotated: the previous token, held by CI, is now dead); DELETE → 200 revoked",
  "evidence": "post_all.py: four 201s and four 200s for member_t1 on these routes; the body reads only get_release(release_id, tenant_id) then writes status_poll_token_hash; CLAUDE.md 'Public release-status endpoint is token-gated … Mint + revoke are Release-Owner/admin, tenant-scoped' | RUN 2 (idor.json): member 14 minted (201) and revoked (200) the status token on a release created by 15; the viewer got 403, the tenant-2 member 404",
  "expected": "only the release owner or an admin can expose a release's decision status to an unauthenticated poller, or invalidate the token CI already holds",
  "observed": "every member of the tenant can do both, for every release; the act is logged but not gated",
  "proposed_fix": "gate at Tier.ADMIN or add the owner check (release.created_by == request.user['id']); correct CLAUDE.md or the code so they agree; add the route to the authz inventory's expected-tier table",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:24:06+00:00",
  "closed_at": "2026-09-21T04:34:43+00:00",
  "closed_by": "triage-round2 batch 1 (authority completion), 2026-09-21",
  "closure": "release/routes.py _release_owner_or_admin: mint, rotate and revoke of the status-poll token are refused (403) unless the caller is the release's creator (releases.created_by) or an admin; CLAUDE.md's security-posture line now states the rule the code enforces and names the route-authority table; proven DB-real on scratch (member 14 on 15's release: 201 before on main @9fbe2e2, 403 after; the owner and the admin still mint and revoke) — tests/integration/test_authority_triage.py"
 },
 {
  "id": "AUD-013",
  "title": "PASS 4: an execution can be enqueued with NO plan — the 'every run starts from a RECORDED plan' invariant (D-486) is not enforced at the enqueue chokepoint",
  "class": "an invariant enforced at some entry points and asserted for all of them (self-reporting control)",
  "severity": "critical",
  "pass": 4,
  "surface": "POST /api/s4-execution-jobs (views.py:4535 → execution_engine/intake.py:enqueue_s4_execution, plan_id Optional=None); the same intake serves POST /claims/<id>/run and /claims/<id>/run-async",
  "reproduction": "on scratch, as an admin (tenant 1): plant an approved claim with an active metadata recipe; POST /api/s4-execution-jobs {test_id, environment_id: 4} with no plan → 202 {job_id: 131, status: queued}; SELECT plan_id FROM s4_execution_jobs WHERE id=131 → NULL. Two earlier attempts were refused for OTHER reasons (environment scope; no executable recipe) — none of the refusals was 'no plan'",
  "evidence": "job 131 queued on scratch with plan_id NULL, created_by 15; intake.enqueue_s4_execution signature `plan_id: Optional[str] = None`; the route's own docstring names a production-confirm gate and an admin gate and no plan gate; D-486 records 'every execution starts from a RECORDED plan … /run retired', and the memory ledger already counts 568 plan-less stamped runs since the re-enable as evidence",
  "expected": "a loud refusal naming the missing plan, at the ONE execution chokepoint, for every entry point",
  "observed": "the release, requirement and schedule entry points plan first; the API and the two claim-level routes hand the intake a job with no plan and it accepts it — the invariant holds by convention of the caller, not by the chokepoint",
  "proposed_fix": "make plan_id REQUIRED in enqueue_s4_execution and refuse without it (PlanRefused), then either point the claim-level routes and the API at the planner (plan-for-one-claim) or retire them as /run was; add a guard test that enqueues without a plan through every route and asserts the refusal. The 568 plan-less runs already on production are the evidence this path is exercised",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T07:28:35+00:00",
  "closed_at": "2026-09-19T06:36:04+00:00",
  "closed_by": "D-494 (merge 1eb64a8, 2026-09-15)",
  "closure": "D-494 (merge 1eb64a8, 2026-09-15): the plan requirement enforced at intake/sync/worker; every S4 path refuses first naming the plan"
 },
 {
  "id": "AUD-014",
  "title": "PASS 4 #2: Evaluate WRITES a recorded GO decision over zero checks for a release whose only declared target is an inactive environment — while the release board says it will refuse",
  "class": "a list must not contradict the page it links to (D-491), and a gate must not read GO over nothing",
  "severity": "critical",
  "pass": 4,
  "surface": "/releases (board card) vs /releases/<id>?tab=decision (quality decision card) — hostile release AUD-release-inactive-env: one requirement (79 approved claims), one target: an environment with is_active=false",
  "reproduction": "plant a release with one requirement of 79 approved claims and declare a single target environment that is inactive (scripts/audit/hostile_world.py); GET /releases → the card reads 'Evaluate will refuse — 79 items in scope are not current · 1 requirement · 0 of 79 functional checks current'; GET /releases/<id>?tab=decision → data-recommendation=\"go\", 'targets (declared): AUD-inactive: 0 of 0 checks current', 'no rule triggered', 'Grading Nothing ungraded'",
  "evidence": "counts.py + the page fragments: the board counts the 79 claims as in-scope-and-not-current; the decision preview scopes to the declared (inactive) target, finds 0 checks, and with nothing to grade recommends GO; the recorded decision row (planted) reads CANNOT DETERMINE beside it; PASS 4 attack #2 on scratch, as admin: POST /releases/<id>/evaluate-decision → a new release_decisions row, recommendation=go, on the inactive-only release (decisions 13 → 14); the preview had already read GO over '0 of 0 checks current'; PASS 4 attack #9 on the NO-SCOPE release (zero requirements, zero claims): Evaluate was not refused — it recorded decision 32, recommendation=go, criteria_met every axis true, policy Plimsol default v1, plan NULL. The inactive-only release recorded decision 33 the same way. Both are the same defect: the policy grades an EMPTY input as satisfied on all six axes",
  "expected": "one answer: an inactive-only target is either refused (no active environment in scope — the D-485 interim) on BOTH surfaces, or evaluated on both; and a preview with zero checks never reads GO",
  "observed": "the list says refuse, the page previews GO, and Evaluate RECORDS GO — a release can be recommended and recorded GO by deactivating its only target environment after declaring it; nothing was checked",
  "proposed_fix": "Evaluate must REFUSE (cannot_determine, no row) when the resolved scope holds zero checks or zero active environments — D9's 'ungraded inputs block GO' applied to an empty input, and D-484's 'Evaluate refuses non-current scope' applied to no scope at all; the board and the tab call the same scope filter (D-491); page tests over a no-scope release and an inactive-only target",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:40+00:00",
  "closed_at": "2026-09-19T06:36:04+00:00",
  "closed_by": "D-496 (merge bbd943f, 2026-09-18)",
  "closure": "D-496 (merge bbd943f, 2026-09-18): a decision requires a non-empty graded scope — the engine refuses before the policy runs; the board and the page from the same call; production's two GO rows audited, none empty"
 },
 {
  "id": "AUD-015",
  "title": "an expired waiver vanishes from the Waivers page, which then reads 'no waivers'",
  "class": "a data shape nobody planned: the aged-out row is hidden rather than shown expired",
  "severity": "medium",
  "pass": 2,
  "surface": "/settings/waivers — with the tenant's only waiver expired (planted: expires_at = now − 1 day, created 3 days ago)",
  "reproduction": "insert a waiver whose expires_at is past (the service refuses a past expiry, so by direct insert — which is exactly what time does to a live one); GET /settings/waivers",
  "evidence": "the page renders data-testid=\"no-waivers\" and nothing else; the waiver's reason and item are absent; no 'expired' count | RUN 2: /settings/waivers as member and admin lists no 'audit: expired' waiver (counts2.json)",
  "expected": "the waiver listed as expired (its item is no longer accepted, and a human should see that the acceptance lapsed and by whom it was given), or at least 'N expired' beside the active list",
  "observed": "the acceptance and its lapse are invisible; the page tells a reviewer there were never any waivers",
  "proposed_fix": "list expired waivers in their own section with the expiry date; the release decision tab's 'N pending, N waived by <actor> until <date>' line already has the vocabulary",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:40+00:00",
  "closed_at": "2026-09-21T09:08:36+00:00",
  "closed_by": "round3 part A (what a pilot client hits first), 2026-09-21",
  "closure": "ROOT CAUSE wider than recorded: /settings/waivers read waivers_for_release(s, None), whose WHERE release_id = NULL matches nothing, so the register listed tenant-wide waivers ONLY — a release-scoped acceptance, active or lapsed, was invisible (the planted one was release-scoped). quality_policy.all_waivers lists every scope and state; the page renders three sections — Active, Lapsed (accepted by whom, recorded by whom and when, lapsed on which date, 'not counted'), Revoked (by whom, when, why); proven DB-real (four planted states listed by state with names and dates; RED on main); screenshots settings_waivers_active_lapsed_revoked.png + PRODUCTION_settings_waivers.png (production holds 0 waivers — the honest render)"
 },
 {
  "id": "AUD-016",
  "title": "the conformance run page answers 200 'the report store is unavailable right now' for a run id that does not exist — and logs a uuid cast error for a non-uuid id",
  "class": "a lookup miss reported as an infrastructure failure",
  "severity": "medium",
  "pass": 2,
  "surface": "GET /runs/conformance/<job_id> (the parameter is untyped)",
  "reproduction": "GET /runs/conformance/abc, /runs/conformance/161, /runs/conformance/00000000-0000-4000-8000-000000000000 as a member",
  "evidence": "all three → 200 with 'UI conformance · Run abc… · Verdict listing · The report store is unavailable right now'; the log carries psycopg2 InvalidTextRepresentation invalid input syntax for type uuid: \"161\" / \"abc\" from the lookup | RUN 2: /runs/<orphan uuid> -> 200 'Run not found — No execution run matches this id.' for every tier",
  "expected": "404 for an id that names nothing; the store's unavailability said only when the store is unavailable",
  "observed": "a bad link reads as an outage; the cast error is swallowed into the same message, so the log and the page disagree about what happened",
  "proposed_fix": "type the parameter <uuid:job_id> so a non-uuid is a 404 at the router; 404 on a miss; keep the 'store unavailable' sentence for a store error only",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:40+00:00",
  "closed_at": "2026-09-21T05:33:06+00:00",
  "closed_by": "triage-round2 batch 3 (a miss is a 404), 2026-09-21",
  "closure": "GET /runs/conformance/<uuid:job_id> — the id is typed at the router (a non-uuid is a 404 there, no cast error reaches the log); ui_report_console.run_report answers found=False when neither an inspection job nor a processing run carries the id, and the page renders its own not-found state ('Run not found — no conformance run matches this id') as a 404; 'The report store is unavailable right now' is rendered for a store error only (available=False) and as a 503. Proven DB-real on scratch (abc / 161 / a fresh uuid); RED on the pre-change tree (200 + the outage sentence for all three). The 6a page test that expected 200 for the nil uuid now expects the page's 404"
 },
 {
  "id": "AUD-017",
  "title": "an unstamped run made today reads 'this run predates run-level environment stamping' — the sentence assumes the run is legacy",
  "class": "a status sentence that explains one cause as if it were the only cause",
  "severity": "low",
  "pass": 2,
  "surface": "/runs/<run_id> and the requirement page's readiness pill, for a run with org_version_seq NULL created after Step 2 shipped",
  "reproduction": "insert an s4_execution_runs row with a NULL stamp dated today (the hostile world does); open the run",
  "evidence": "pill title: 'Freshness unknown — this run predates run-level environment stamping. Run again to establish current readiness.' on a run from 5 minutes ago",
  "expected": "the CANNOT_DETERMINE / unstamped state named without asserting when or why the stamp is missing",
  "observed": "a run that lost its stamp for any other reason is described as pre-dating the feature",
  "proposed_fix": "say 'this run carries no environment stamp' and, if the run predates Step 2's deploy, add 'it predates run-level stamping'; the date is on the row",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:41+00:00",
  "closed_at": "2026-09-21T09:08:36+00:00",
  "closed_by": "round3 part A (what a pilot client hits first), 2026-09-21",
  "closure": "readiness.py: SENTENCE_UNSTAMPED states the fact — 'this run carries no environment stamp' — and SENTENCE_UNSTAMPED_LEGACY adds 'it predates run-level stamping' only when the run's own timestamp (now carried on the resolution as run_at, from COALESCE(finished_at, started_at) in both resolvers) is earlier than STAMPING_DEPLOYED_AT = Step 2's deploy (D-484 merge 0fe6848, 2026-09-08 06:14:12Z); proven DB-real (a run dated today: no clause; a run dated 2026-09-01: the clause) and in the unit tests; RED on main (every unstamped run called legacy); screenshots run_detail_unstamped_today.png, run_detail_unstamped_legacy.png, PRODUCTION_run_detail_legacy_unstamped.png (run 562682d5 of 2026-08-04 carries the clause; production holds 0 unstamped runs after Step 2)"
 },
 {
  "id": "AUD-018",
  "title": "`claim_sets.status = 'revoked'` is read by the approval path and written by nothing",
  "class": "a state the reader handles that no producer emits",
  "severity": "low",
  "pass": 2,
  "surface": "primeqa/test_representation/claim_sets.py:223 (approve refuses a 'revoked' set); no code path sets the status; only members are revoked (revoke_member)",
  "reproduction": "grep -n \"status = 'revoked'\\|'revoked'\" primeqa/test_representation/claim_sets.py — one read, zero writes; the hostile world planted the state by direct insert and /settings/claim-sets rendered it 'revoked' correctly",
  "evidence": "the reader coped (the page shows the cloned set as revoked, 0 of 152 members), so this is not a defect on a page; it is a vocabulary the product speaks but cannot produce",
  "expected": "either a service that revokes a whole set (audited, with a reason) or the dead branch removed",
  "observed": "a status that can only arrive by hand-editing the table",
  "proposed_fix": "add revoke_claim_set (all members + the set, one audited act) or delete the branch; either way, a test that every status value read has a writer",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T11:59:41+00:00",
  "closed_at": "2026-09-21T09:08:36+00:00",
  "closed_by": "round3 part A (what a pilot client hits first), 2026-09-21",
  "closure": "DECIDED with a lean: DELETE the branch. Members are revoked one by one through revoke_member, which deprecates the claim with a reason; a whole-set revoke would deprecate every claim of a set in one act — an act nobody asked for — and the state could only arrive by hand-editing the table. The dead read in approve_claim_set is deleted. GUARD tests/unit/test_status_vocabulary.py: every status literal the product reads on claim_sets and quality_policies (found by regex over the source, never listed) has a writer; a planted read of an unwritten value fails the gate; RED on main ('revoked' read, never written)"
 },
 {
  "id": "AUD-019",
  "title": "quality-policy activation is gated at MEMBER, below the Settings surface that hosts it",
  "class": "capability enforced at the route below the audience the surface declares",
  "severity": "medium",
  "pass": 3,
  "surface": "POST /settings/quality-policy/activate — views.py:5074 @require_tier(Tier.MEMBER); the Settings nav item is shown only when can_see_settings (superadmin or a manage_* capability); D-488: 'human activation (audited, CLI or Settings) before any policy grades' — the control AK had to reach for a CLI to use",
  "reproduction": "as a tester (MEMBER): POST /settings/quality-policy/activate with policy_id=<a draft> — the route admits the tier; on the hostile world the member's five garbage POSTs reached the route (302 back to the page) and only failed to activate because none named a draft",
  "evidence": "post_all.py member_t1 → 302 /settings/quality-policy for every variant (not the '/' deny redirect); the route decorator reads Tier.MEMBER; the page that offers the button is behind the admin-only Settings nav",
  "expected": "the act that decides what grades every release sits at the same tier as the surface that offers it (ADMIN), or the audience is stated and tested",
  "observed": "a member who types the URL can activate — and thereby retire the active policy (activate retires the previous active version in the same act)",
  "proposed_fix": "gate at Tier.ADMIN (matching /settings/*), and add the route to the authz expected-tier table so the gap cannot reopen",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:06:51+00:00",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @8f990ec",
  "closure": "POST /settings/quality-policy/activate gated at Tier.ADMIN; the route-authority table (tests/unit/test_route_authority_table.py) proves it behaviourally and goes red on the pre-change tree"
 },
 {
  "id": "AUD-020",
  "title": "a member revoked a waiver another user recorded, with an EMPTY revocation reason",
  "class": "an act with no owner check and no reason check on the undo side",
  "severity": "medium",
  "pass": 3,
  "surface": "POST /settings/waivers/<id>/revoke and /releases/<id>/waivers/<id>/revoke — the planted waiver (created_by 15, reviewer 15) revoked by user 14 with reason ''",
  "reproduction": "as member 14: POST /settings/waivers/<planted>/revoke with an empty form → 302 to the waivers page; SELECT revoked_by, revocation_reason FROM quality_waivers → 14, ''",
  "evidence": "scratch state after the hostile POST sweep: revoked_by=14 reason='' on a waiver whose creator and reviewer are user 15; record_waiver refuses a missing reason on creation ('a waiver carries its reason') — the undo does not",
  "expected": "revocation by the reviewer, the creator or an admin, with a reason recorded, since a waiver is 'a named human accepting a known state' (D-488) and its withdrawal is the same kind of act",
  "observed": "any member can silently withdraw any acceptance in the tenant, and the ledger shows who but not why",
  "proposed_fix": "require a non-empty revocation_reason (the column exists) and an owner-or-admin check in revoke_waiver; a test that a member's empty revoke is refused",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:06:51+00:00",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @8f990ec",
  "closure": "quality_policy.revoke_waiver refuses a non-owner non-admin (reviewer, recorder or admin) and an empty reason before any write; the route passes rank(role) >= ADMIN; ck_quality_waivers_revocation_reason refuses an empty reason at the table (migration 20260919_0010)"
 },
 {
  "id": "AUD-021",
  "title": "planning accepts an empty form and records an immutable plan row — twelve garbage plans from one member's five-body sweep",
  "class": "no validation at the boundary — an immutable ledger row from no input",
  "severity": "low",
  "pass": 1,
  "surface": "POST /plans (tenant-wide), POST /releases/<id>/plan, POST /requirements/<id>/plan — each recorded a run_plans row for the empty, maximal, wrong-type and missing-required bodies",
  "reproduction": "as a member: POST /plans with an empty body → 302 to the new plan; SELECT count(*) FROM run_plans WHERE planned_by = <member> → grows by one per POST",
  "evidence": "12 run_plans rows planned_by 14 after the hostile POST sweep; run_plans are never deleted by design (D-486), so every garbage POST is a permanent row",
  "expected": "a plan needs a stated environment (or the resolved default named back) and the empty form is refused; a missing environment is one of the planner's own refusal reasons",
  "observed": "the tenant-wide and per-scope plan forms record on nothing; the ledger fills with plans nobody asked for",
  "proposed_fix": "require environment_id at the route (400 / flash) before planner.plan; keep the planner's own refusals for the resolved shape",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:06:51+00:00",
  "closed_at": "2026-09-21T05:07:36+00:00",
  "closed_by": "triage-round2 batch 2 (boundary validation), 2026-09-21",
  "closure": "CORRECTED SCOPE on reproduction (D-490, sweep before believing): at the audited tree the tenant-wide POST /plans and POST /requirements/<id>/plan ALREADY refused an empty or wrong-typed environment at the route (302 back with 'Pick an environment you have access to.', no row) — the recorded garbage came from POST /releases/<id>/plan, which takes no environment field and recorded a plan for EVERY body when the release had no declared target and no evidence to fall back on (targets_source fallback:evidence-active resolving to []): an immutable never-deleted row with zero targets and zero jobs. Fixed where the shape is known — the planner refuses a release plan whose targets resolve to nothing (REFUSE_NO_TARGET, 'This release has no target environment — declare a target first.') before any row is written; the route flashes the sentence. Proven DB-real: a release with no target and no evidence records no run_plans row on any of the three doors; RED on the pre-change tree (a row per POST)"
 },
 {
  "id": "AUD-022",
  "title": "a requirement whose every claim is deprecated shows no trace of them: 'no functional check', no chip, no count",
  "class": "a data shape nobody planned: the fully-retired scope reads as never-tested",
  "severity": "low",
  "pass": 2,
  "surface": "/requirements (board row) and /requirements/<id> for the hostile requirement AUD-DEP (five claims, all deprecated)",
  "reproduction": "plant a requirement with five approved claims and deprecate all five (scripts/audit/hostile_world.py); GET /requirements → the row reads 'functional: no functional check', no approved/draft chip; GET the requirement → zero claim rows, no 'N deprecated'",
  "evidence": "board_rows excludes status='deprecated' by design (D-219: retired from the corpus) and the page lists live claims only; nothing says five existed | RUN 2: the requirements list row for the all-deprecated requirement reads 'no tests yet … Generate' while its page lists 5 deprecated",
  "expected": "the retirement is visible: 'no live test case · 5 deprecated' — a reviewer can tell never-tested from all-retired",
  "observed": "both read as never-tested",
  "proposed_fix": "carry a deprecated count into the board row and the page header (the D-219 exclusion stays for grading)",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:06:52+00:00",
  "closed_at": "2026-09-21T09:08:36+00:00",
  "closed_by": "round3 part A (what a pilot client hits first), 2026-09-21",
  "closure": "_count_claims_by_requirement_status counts deprecated claims under their own key and never in the total (the total stays the live plan, D-219/D-269); the board row reads 'no live test case · N deprecated' when everything is retired and shows an 'N deprecated' chip beside live chips otherwise; read_requirement_claims returns the deprecated count and the page header reads '(0 tests · 5 deprecated)' with the empty state 'No live test case · 5 deprecated (retired from the plan…)'; proven DB-real (an all-deprecated and a mixed requirement; RED on main: 'no tests yet'); screenshots requirements_board_all_deprecated.png, requirement_all_deprecated_header.png, requirement_mixed_header.png, PRODUCTION_requirements_board_deprecated_chips.png (req-320: 79 deprecated beside 33 live), PRODUCTION_requirement_320_header.png"
 },
 {
  "id": "AUD-023",
  "title": "a member deactivated a release target another user declared — the undo of a declared act carries no owner check",
  "class": "an act with no owner check and no reason check on the undo side",
  "severity": "medium",
  "pass": 3,
  "surface": "POST /releases/<id>/targets/<env>/remove — the hostile release's only target (declared_by 15) removed by member 14 with a valid body",
  "reproduction": "as member 14: POST /releases/<id>/targets/<env>/remove {reason} → 302; SELECT active, deactivated_by FROM release_targets → false, 14",
  "evidence": "scratch state after the probe: active=false deactivated_by=14 on a target declared by 15; the same member could not finalise 15's decision (refused), so the two undo paths differ",
  "expected": "a declared target is removed by its declarer or an admin (D-486 records the actor on the declaration for exactly this), or the audience is stated",
  "observed": "any member can reshape another user's release scope, and the release then reads as if the declarer had changed their mind",
  "proposed_fix": "owner-or-admin on target removal (declared_by == user or ADMIN); a test that a member's removal of another user's target is refused",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:07:58+00:00",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @8f990ec",
  "closure": "planner.remove_target refuses a non-declarer non-admin and an empty reason before any write; the route passes rank(role) >= ADMIN; ck_release_targets_deactivation_reason at the table (migration 20260919_0010)"
 },
 {
  "id": "AUD-024",
  "title": "a claim, run or job id that names nothing renders a 200 'not found' page instead of a 404",
  "class": "a lookup miss reported as a page (same cause as AUD-016)",
  "severity": "low",
  "pass": 3,
  "surface": "/claims/<uuid>, /claims/<uuid>/panel, /claims/<uuid>/run-status/<job>, /runs/<uuid>, /runs/conformance/<id> — observed from a tenant-2 member asking for tenant-1 ids (the isolation sweep), which is the same as any bad link",
  "reproduction": "as any member: GET /claims/<a uuid that exists nowhere in your tenant> → 200 'Test case not found — it may have been superseded or removed'; GET /runs/<such a uuid> → 200 'Test run' shell",
  "evidence": "scripts/audit/isolation.py: 108 cross-tenant requests, zero tenant-1 markers in any body, zero 2xx acts — isolation HOLDS; eight 200s were all not-found shells or empty lists",
  "expected": "404 for a miss, so monitors, crawlers and CI tell a dead link from a page",
  "observed": "a 200 that says 'not found' in prose",
  "proposed_fix": "return 404 with the same template (the shared-dashboard route already does this for a bad token)",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:09:40+00:00",
  "closed_at": "2026-09-21T05:33:06+00:00",
  "closed_by": "triage-round2 batch 3 (a miss is a 404), 2026-09-21",
  "closure": "a miss is a 404 with the page's own template: /claims/<uuid> (detail), /claims/<uuid>/panel, /claims/<uuid>/run-status/<job>, /runs/<uuid> render their existing not-found state with status 404 (found=False from the console reads; the store's unavailability is not a miss). The same rule applied to every other id-taking page route of the url_map — the detail/edit pages that silently redirected to their list on a miss (connections, environments, groups, releases, requirements, settings/users, users) now answer the themed 404, and the two status fragments (/environments/<id>/sync-substrate/status, /requirements/<id>/test-plan-status) answer 404 for an unknown subject. THE GUARD tests/integration/test_miss_is_404.py: generated from the url_map, every id-taking page route (22) fetched with an id that names nothing must answer 404 — RED on the pre-change tree with 19 of 22 not 404 (five 200s in prose, twelve silent 302s, two 200 fragments); GREEN now, 11 tests. The three retired v1 id spaces (/results/<int>, /runs/<int>, /suites/<int>) are named as redirects to their live successor and asserted to land on a 200"
 },
 {
  "id": "AUD-025",
  "title": "orphan routes: the sections page and the tenant-wide plan form are reachable but nothing links to them; five retired aliases redirect without a link; 71 of 78 API routes have no UI caller",
  "class": "reachable, unlinked — a surface nobody can find is a surface nobody tests",
  "severity": "low",
  "pass": 1,
  "surface": "/sections (GET, a live page), POST /plans (the tenant-wide plan), POST /requirements/<id>/run-substrate (already refusing), and the bookmark redirects /results, /results/<id>, /suites/<id>, /tickets; two more (/claims/<id>/panel, /environments/<id>/sync-substrate/status) are reached by JS-built URLs and are not orphans",
  "reproduction": "scripts/audit/deadlinks.py's target set (every href/action/hx-*/data-*/fetch literal in templates and static JS) matched against the live url_map; a page-facing rule with no matching target is listed",
  "evidence": "128 page-facing rules, 9 with no static target; 78 /api rules, 71 never named by a template or static JS — the API is a programmatic surface (CI, CLI, tests), so those are coverage facts, not defects",
  "expected": "every page either has a way in or is retired; a route kept only for bookmarks says so in code",
  "observed": "the sections page is live and unreachable from the nav; the tenant-wide plan can only be recorded by hand-crafting a POST (and, per AUD-021, from an empty one)",
  "proposed_fix": "link /sections from Settings or retire it; retire POST /plans or give it a form; keep the five aliases but mark them as redirect-only in the inventory",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:10:36+00:00",
  "note": "round 3 part D: traced/measured read-only and written up with options and a defended lean in docs/audit/DECISION_MEMO.md — AK decides.",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part D, 2026-09-22",
  "closure": "the five bookmark aliases (/results, /results/<int>, /suites, /suites/<int>, /tickets) are KEPT and declared REDIRECT_ONLY in tests/unit/test_route_authority_table.py with their successors; a unit proof shows each answers a redirect to its successor for a signed-in viewer; the miss sweep reads the id-taking ones from that one source. They are not orphans: the inventory reads this set."
 },
 {
  "id": "AUD-026",
  "title": "PASS 4 #1: a PASS verdict for a run that does not exist is insertable — s6_interpretations.run_id has no foreign key to s4_execution_runs",
  "class": "an invariant the ledger states that the schema does not hold (the readers happen to join through runs)",
  "severity": "medium",
  "pass": 4,
  "surface": "tenant schema: s6_interpretations (verdicts) → s4_execution_runs (runs); pg_constraint lists NO foreign key on run_id",
  "reproduction": "INSERT INTO s6_interpretations (run_id, recipe_id, claim_test_id, outcome, verdict, detail) VALUES (<random uuid>, <random uuid>, <a claim>, 'passed', 'passed', '{}') → accepted; SELECT count(*) FROM s6_interpretations i WHERE NOT EXISTS (SELECT 1 FROM s4_execution_runs r WHERE r.run_id = i.run_id) → 1",
  "evidence": "D-466 records that PASS requires positive attestation over captured evidence; that holds in the interpreter's writer, not in the table. The decision path reads verdicts through a join on s4_execution_runs (_COUNTED_RUNS_BULK_SQL), so the orphan is invisible to grading today — the invariant is held by every reader's join, not by the schema",
  "expected": "a verdict cannot exist without the run it interprets: a foreign key, and a guard that no verdict row is orphaned",
  "observed": "a PASS with no run behind it sits in the table; any reader that lists verdicts without the join would count it",
  "proposed_fix": "add the FK (ON DELETE CASCADE with the run's own retention), verify zero orphans on production before applying, and add a schema test that every verdict's run exists",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-15T12:14:58+00:00",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @59e87e9",
  "closure": "fk_s6_interpretations_run (s6_interpretations.run_id -> s4_execution_runs.run_id, NO ACTION) in migration 20260919_0010 after a read-only zero-orphan proof on production (3,691 rows, 0 orphans); proven by attempting an orphan insert and a run delete; the run_id census gate (tests/integration/test_constraints_triage.py) fails on any new unledgered run_id column"
 },
 {
  "id": "AUD-027",
  "title": "the pre-commit hook ended its first step with `exit 0`, so the undefined-name step it advertised never ran",
  "class": "a control that reports its own correctness",
  "severity": "medium",
  "pass": "triage",
  "surface": ".githooks/pre-commit — `exit 0` after the decision-number step; the pyflakes block sat below it, unreachable",
  "reproduction": "stage a Python file with an undefined name and commit: the hook exits 0 before the pyflakes step; only the unit gate (tests/unit/test_no_undefined_names.py) catches it",
  "evidence": "the file's own text: `exit 0` on the line before the pyflakes block; on first revival the step then called the SYSTEM python3 (no pyflakes) and refused this batch's own commit",
  "expected": "every hook step runs; one exit at the end",
  "observed": "a dead step documented as live (D-495)",
  "proposed_fix": "steps fall through to one final exit; the venv interpreter when present",
  "status": "closed",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @4da6e32 + @8f990ec",
  "closure": "the hook's steps fall through to one final exit; the dead-link step added as step 3; `$py` = venv/bin/python when present; proven red with a dead link staged",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T06:35:37+00:00"
 },
 {
  "id": "AUD-028",
  "title": "three more tenant tables carry a run_id with no foreign key — and two of them already hold orphans on production",
  "class": "guarded by convention, not by constraint (AUD-026's siblings)",
  "severity": "medium",
  "pass": "triage",
  "surface": "tenant schema: s4_created_records.run_id (3,547 rows, 3 orphan run_ids), repair_proposals.run_id (140 rows, 4 orphans), s6_reinterpretations.run_id (50 rows, 0 orphans) — production, read-only under the proven guard, 2026-09-19",
  "reproduction": "SELECT count(*) FROM <t> x WHERE NOT EXISTS (SELECT 1 FROM s4_execution_runs r WHERE r.run_id = x.run_id)",
  "evidence": "pg_constraint lists no FK on any of the three; the counts above; the repair-gate suite's own fixtures plant proposals on made-up run ids (uuid4()) — the FK would refuse them, so the suite documents the gap",
  "expected": "a foreign key on each, once the orphans are explained",
  "observed": "orphans exist on two tables, so the rule for AUD-026 applies: stop and report, never delete",
  "proposed_fix": "explain the 3 + 4 orphan rows (a run deleted from under them? a race?), decide their fate with AK, then add the FKs; s6_reinterpretations can take its FK now",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T06:35:37+00:00",
  "note": "named in the run_id census ledger (tests/integration/test_constraints_triage.py::RUN_ID_WITHOUT_FK) so the gate stays honest until they are closed | round 3 part D: traced/measured read-only and written up with options and a defended lean in docs/audit/DECISION_MEMO.md — AK decides. | round 4 part A (2026-09-22): the write order is fixed at the root (the run row exists before any record is provisioned; an interrupted run closes as errored with the shutdown reason — proven on scratch with SIGTERM between two provisioning steps: zero orphans, one failed run), the three keys are added (two NOT VALID, permanently, by AK's ruling), the seven rows are named exceptions in the census ledger; the four July proposals' withdrawal is the GATED data act — AK's GO at the HOLD. Status stays open until that act is recorded."
 },
 {
  "id": "AUD-029",
  "title": "the repair apply chokepoint (_apply) carried no guard of its own — called directly with a SPECULATIVE row it wrote a new recipe version, promoted it and queued a re-verify run",
  "class": "guarded by convention, not by constraint",
  "severity": "medium",
  "pass": "4 (attack #5)",
  "surface": "primeqa/intelligence/repair_agent.py::_apply — the verdict check lived in decide_proposal and auto_apply_proposals, its two callers",
  "reproduction": "scratch, gate switch ON: _apply(1, <SPECULATIVE recipe_edit row>) -> recipe versions 1 -> 2 (actor s8, promoted), job queued; _stamp('applied') then refused by the new table trigger",
  "evidence": "the four reachable paths (route, decide_proposal, auto pass; no API exists) all refused by the callers' check; only the direct internal call mutated",
  "expected": "the chokepoint refuses first (D-494's shape)",
  "observed": "a caller-side guard: convention",
  "proposed_fix": "_apply re-reads the switch, verdict, grounding and applicability and refuses before any write; the table refuses the stamp",
  "status": "closed",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @5e84325",
  "closure": "contained: _apply refuses first; test_repair_gate::test_c4; the table trigger repair_proposals_apply_guard",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T06:35:37+00:00"
 },
 {
  "id": "AUD-030",
  "title": "CONVENTIONS.md's gate paragraph was garbled by the D-495 docs insert — the pyflakes text sat inside the decision-number sentence",
  "class": "scripted edit at the wrong anchor",
  "severity": "low",
  "pass": "triage",
  "surface": "docs/CONVENTIONS.md lines 212-222 (before 4da6e32)",
  "reproduction": "read the paragraph: 'enforced by Undefined-name gate: … `scripts/check_decision_numbers.py` (runs in the unit gate suite…)'",
  "evidence": "the 6d09603 docs commit inserted the gate text at the wrong line",
  "expected": "one bullet per gate",
  "observed": "two sentences interleaved",
  "proposed_fix": "reorder",
  "status": "closed",
  "closed_at": "2026-09-19T06:35:37+00:00",
  "closed_by": "triage-constraints @4da6e32",
  "closure": "reordered while adding the dead-link gate bullet",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T06:35:37+00:00"
 },
 {
  "id": "AUD-031",
  "title": "the Sections page is reachable only by typing its URL — no template, nav entry or redirect links /sections",
  "class": "orphan surface: a live page nothing links to",
  "severity": "medium",
  "pass": "run2",
  "surface": "/sections (views.py:4728, MEMBER+ acts on it: rename, delete, restore) — scripts/audit/orphans.py over the repaired link graph (templates, static JS, navigation.py, Python redirect())",
  "reproduction": "grep every href/hx-*/action/JS literal/nav entry for '/sections': none; GET /sections renders 200 for every tier on scratch and production",
  "evidence": "orphans.json: 80 unlinked rules of 211, 9 non-API; /sections is the only full PAGE among them without a redirect or a polling caller",
  "expected": "a page users can reach from the product, or a page retired with its acts",
  "observed": "a working page with admin acts (rename/delete/purge) that only a remembered URL reaches — AUD-004's dead 'View TCs' lived on it unseen",
  "proposed_fix": "link it from Settings (it manages the requirement tree) or retire it",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "note": "round 3 part D: traced/measured read-only and written up with options and a defended lean in docs/audit/DECISION_MEMO.md — AK decides.",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part D, 2026-09-22",
  "closure": "/sections is linked from the Settings sidebar (settings/base.html, a 'Sections' item after Groups, for every signed-in user; its acts are gated inside the page). Screenshot-gated."
 },
 {
  "id": "AUD-032",
  "title": "/suites and /suites/<id> redirect to /claims, a path retired in 6a — a 302 that lands on a 404; navigation.py still names /claims, /run, /runs/new, /suites",
  "class": "retired redirect target",
  "severity": "low",
  "pass": "run2",
  "surface": "views.py:5498 suites_redirect -> redirect('/claims'); primeqa/core/navigation.py active_also_for ('/claims', '/tickets') and the legacy capability map ('/run', '/runs/new', '/suites')",
  "reproduction": "GET /suites -> 302 /claims -> 404 (scratch and production, every tier)",
  "evidence": "url_map has no '/claims' rule (only /claims/<uuid>/...); the sweep's orphan pass lists /suites/<int:suite_id> and the nav config's paths",
  "expected": "a retired path redirects to its live successor or answers 410",
  "observed": "a chain that ends on the 404 page",
  "proposed_fix": "redirect /suites to /requirements (the claims live there since 6a); drop the dead paths from navigation.py",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "closed_at": "2026-09-21T05:33:06+00:00",
  "closed_by": "triage-round2 batch 3 (a miss is a 404), 2026-09-21",
  "closure": "/suites and /suites/<id> redirect to /requirements (the claims' home since 6a) and the successor answers 200; navigation.py's landing-page map drops /run, /runs/new and /suites (a preferred landing page naming one would land on a redirect chain) — /claims stays as a highlight PREFIX because /claims/<uuid> is the live claim detail page (6a ruling 6), /tickets and /results stay as redirect prefixes (6a: 'including the redirected ones'). THE GUARD tests/unit/test_navigation_paths.py: every landing path is a rule whose view renders, every highlight prefix names a rule that answers, every rendered nav url renders (the two never-rendered placeholders named), the suites redirect targets /requirements, and a planted retired path fails the gate — RED on main (2 of 5), GREEN now"
 },
 {
  "id": "AUD-033",
  "title": "two act routes have no caller anywhere in the product: POST /plans (the tenant-wide plan) and POST /requirements/<id>/run-substrate (refusing without a plan since D-494)",
  "class": "orphan act: a gated write route nothing links to",
  "severity": "low",
  "pass": "run2",
  "surface": "views.py:2800 plan_tenant_wide (MEMBER), views.py:3102 requirements_run_substrate (MEMBER)",
  "reproduction": "orphans.py: no form action, hx-post or JS literal targets either; both answer a POST (the second with the D-494 refusal)",
  "evidence": "the repaired link graph: every template action/hx-* target resolved; neither path appears; the only references are their own decorators",
  "expected": "an act is either reachable from a page or retired",
  "observed": "live acts reachable only by crafted requests — the run-substrate one refuses (D-494); the tenant-wide plan CREATES a plan for whoever posts",
  "proposed_fix": "retire both, or give the tenant-wide plan its surface",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "note": "round 3 part D: traced/measured read-only and written up with options and a defended lean in docs/audit/DECISION_MEMO.md — AK decides.",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part D, 2026-09-22",
  "closure": "POST /plans and POST /requirements/<id>/run-substrate answer 410 Gone with one line naming the successor (views.RETIRED_ROUTE_NOTE: the release plan / the schedule; the requirement plan), for every signed-in caller alike; an anonymous caller still meets the login redirect. The authority table's two rows read RETIRED; the gate tests/unit/test_retired_routes_410.py is RED on main (6 of 8)."
 },
 {
  "id": "AUD-034",
  "title": "71 API routes have no in-product caller: the JSON API surface (/api/releases, /api/requirements, /api/environments, /api/groups, /api/connections, /api/auth/users, /api/patterns, /api/facts, /api/dependencies, /api/metadata, /api/step-schema, …) is reached only by external clients, and no client is documented",
  "class": "API endpoints that outlived their UI (reachable code nothing links to)",
  "severity": "low",
  "pass": "run2",
  "surface": "orphans.json orphan_routes filtered to /api/: 71 rules — 35 with no tier in any closure (auth-only GETs: /api/environments, /api/requirements, /api/releases, /api/patterns, /api/facts, /api/dependencies, /api/metadata, /api/step-schema, /api/groups reads …), 18 MEMBER writes, 29 ADMIN writes",
  "reproduction": "the UI is server-rendered forms + htmx; the JSON API is called by nothing in primeqa/templates or primeqa/static (the two htmx polls and the drawer are the exceptions and are linked)",
  "evidence": "a viewer's GET /api/environments and /api/requirements answer 200 on production (SEC-1 redaction holds: no config in the body)",
  "expected": "an API surface with a named consumer, or retired with a 410",
  "observed": "71 endpoints kept alive for an unnamed consumer, each a gate to keep honest (AUD-012, AUD-036 live here)",
  "proposed_fix": "name the consumer (CI? a future SDK?) and prune the rest — or freeze the list as the public contract and gate-test it as a table",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "note": "round 3 part D: traced/measured read-only and written up with options and a defended lean in docs/audit/DECISION_MEMO.md — AK decides.",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part D, 2026-09-22",
  "closure": "every /api request is recorded (migrations/074_api_request_log.sql, primeqa/shared/request_log.py): the time, tenant/user/role from the token the gate read, the caller kind (anonymous / cookie / bearer / webhook), method, the url_map RULE (never the path), endpoint, status, duration, and the rule's declared minimum tier (primeqa/core/authz_gates.min_tier_by_rule — the oracle's walk, moved into the package). Never a body, query string, header or token (asserted). Written by a bounded queue flushed every 2 s as one INSERT on a daemon thread and at exit, so no request pays the write; the disclosed loss is at most one flush window on a hard kill, drops are counted. Kept 30 days: the scheduler's api_request_log_prune_tick deletes older rows daily. NO route is pruned on this evidence yet (the memo's ruling)."
 },
 {
  "id": "AUD-035",
  "title": "POST /api/sections/<id>/purge on a section that still holds requirements answers 500 — the foreign key is the validator",
  "class": "no validation at the boundary — the database constraint is the validator (AUD-011's class)",
  "severity": "medium",
  "pass": "run2",
  "surface": "test_management/routes.py:153 purge_section -> ForeignKeyViolation on requirements.section_id",
  "reproduction": "as admin: POST /api/sections/<hostile section>/purge with any body -> 500 (four of five bodies; the fifth is missing-CSRF 403)",
  "evidence": "posts_own.json: 4 x 500 on the purge route; posts_own.err: 'update or delete on table \"sections\" violates foreign key constraint \"requirements_section_id_fkey\"'",
  "expected": "409 'the section still holds N requirements' before the DELETE",
  "observed": "an unhandled DataError and the generic 500 page",
  "proposed_fix": "count the children first; refuse with the count",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "closed_at": "2026-09-21T05:07:36+00:00",
  "closed_by": "triage-round2 batch 2 (boundary validation), 2026-09-21",
  "closure": "TestManagementService.purge_section counts the requirements still referencing the section FIRST (live and soft-deleted alike — both still reference it) and refuses with the count as a 409 CONFLICT ('The section still holds N requirement(s) — move or purge them first.', details.requirements = N) before any DELETE; proven DB-real (a planted section with one requirement: 409, the section still there); RED on the pre-change tree (500 ForeignKeyViolation)"
 },
 {
  "id": "AUD-036",
  "title": "decision finalisation has two gates for one act: the API route is admin-only, the web route admits any MEMBER (with the more-permissive-needs-admin rule) — a member finalised another user's release decision as NO GO",
  "class": "one act, two gates",
  "severity": "medium",
  "pass": "run2",
  "surface": "release/routes.py:233 finalize_decision @require_role('admin') vs views.py:4987 release_decision_final @require_tier(Tier.MEMBER)",
  "reproduction": "as member 14: POST /releases/<15's release>/decisions/<id>/final final_decision=no_go reason=... -> 302 'Final decision recorded: NO GO.'; release_decisions.final_decision NULL -> no_go, decided_by 14",
  "evidence": "idor.json: row before ['None','None'] after ['no_go','14']; the viewer and the tenant-2 member were refused; the API route would have refused the same member with 403",
  "expected": "one minimum tier for the act, wherever it is offered (the route-authority table of D-497 is where it belongs)",
  "observed": "the answer depends on which door the member uses",
  "proposed_fix": "decide the rule (member records the human decision? admin only?) and put both routes in the authority table",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "closed_at": "2026-09-21T04:34:43+00:00",
  "closed_by": "triage-round2 batch 1 (authority completion), 2026-09-21",
  "closure": "ONE minimum tier for finalisation: ADMIN on both doors — views.py release_decision_final moves from Tier.MEMBER to Tier.ADMIN (the more-permissive-needs-a-reason rule retained), the API route was already admin; the final-decision form is rendered for admin/superadmin only; both routes carry an entry in tests/unit/test_route_authority_table.py, which was RED on the pre-change tree for the web row (a member passed the gate) and is GREEN now; proven DB-real (member 14 refused on both doors, row unchanged)"
 },
 {
  "id": "AUD-037",
  "title": "a member unlinks a surface another user declared — the undo of a declared act carries no owner check (the AUD-023 shape, on surface links)",
  "class": "guarded by convention, not by constraint (the AUD-020/023 class)",
  "severity": "medium",
  "pass": "run2",
  "surface": "views.py:2663 requirement_surface_unlink (MEMBER) -> requirement_surface_console.unlink_surface -> surface_links.unlink: no declared_by check",
  "reproduction": "as member 14: POST /requirements/<id>/surfaces/<link declared by 15>/unlink reason=... -> 302 'Unlinked /x — 72 conformance checks removed from the plan.'; requirement_surface_links.active true -> false",
  "evidence": "idor.json row; D-485 recorded declared_by on the link for exactly this",
  "expected": "declarer-or-admin, with a reason — the rule D-497 gave targets and waivers",
  "observed": "any member undoes any declaration",
  "proposed_fix": "the D-497 shape: refuse in the service, pass rank(role) >= ADMIN from the route, add the route to the authority table",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "closed_at": "2026-09-21T04:34:43+00:00",
  "closed_by": "triage-round2 batch 1 (authority completion), 2026-09-21",
  "closure": "the D-497 shape: surface_links.unlink refuses a non-declarer non-admin (not_declarer) and an empty reason (no_reason) BEFORE any write; the console returns the typed refusal; the route passes rank(role) >= ADMIN; the reason is stored trimmed; migration 20260921_0010 adds ck_requirement_surface_links_deactivation_reason (REJECTING — proven: without the CHECK an empty-reason deactivation LANDED in a rolled-back transaction, with it CheckViolation); proven DB-real on scratch (member 14 unlinked 15's surface on main @9fbe2e2 — 'Unlinked /x — 72 conformance checks removed' — and is refused now); unit + integration + table-level tests"
 },
 {
  "id": "AUD-038",
  "title": "a DEACTIVATED user keeps acting for the life of the access token (30 minutes): users.is_active is never read on a request, and the API deactivate route commits the flag directly, bypassing the service that revokes refresh tokens",
  "class": "self-reporting control — the seventh: a status flag that reports 'out' while the session acts",
  "severity": "high",
  "pass": "run2",
  "surface": "views.py:1359 api_deactivate_user (sets u.is_active=False, db.commit(); never calls AuthService.update_user, whose 'revoke on is_active=false so disabled users can't ride existing tokens to expiry' therefore does not run); core/auth.py require_auth and views.py login_required read the JWT only; ACCESS_TOKEN_EXPIRY = 30 minutes",
  "reproduction": "scratch: as admin POST /api/users/14/deactivate -> 204, users.is_active=false; then with user 14's still-valid cookie: GET /requirements 200, GET /api/releases 200, POST /requirements/<id>/edit 302 and the row CHANGED ('deactivated user still edits'); the user's two live refresh_tokens rows stayed unrevoked after the API deactivate; POST /api/auth/refresh as the deactivated user -> 401 (refresh does check the flag), so the window is the access token's 30 minutes",
  "evidence": "claims_probe.json; the refresh-token proof; CLAUDE.md's own text: 'Role downgrade → revokes all refresh tokens so stale access-tokens expire quickly'",
  "expected": "deactivation ends the session: the flag is read at the auth chokepoint (one indexed read per request, or a short-lived cache), and every deactivate path revokes refresh tokens",
  "observed": "an offboarded user acts for up to 30 minutes, and via the API route their refresh tokens are left live (harmless only because refresh re-checks the flag)",
  "proposed_fix": "read users.is_active in require_auth/login_required (fail closed); route the API deactivate through the service; a guard test that a deactivated session is refused on the next request",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "closed_at": "2026-09-19T11:24:37+00:00",
  "closed_by": "D-499 (merge 75747fd)",
  "closure": "users.is_active read at the one auth chokepoint every request passes (fail closed); the API deactivate/activate routes go through the service; the last-superadmin guard in the service; the revocation failure logged; the AST guard test_deactivation_paths.py"
 },
 {
  "id": "AUD-039",
  "title": "any member can revoke any other member's shared-dashboard link — the revoke route checks the tenant only (code read; not attempted: the table is absent on scratch and revocation on production is a write)",
  "class": "guarded by convention, not by constraint (the AUD-020/023 class)",
  "severity": "medium",
  "pass": "run2",
  "surface": "views.py:528 api_dashboard_share_revoke @require_tier_api(MEMBER): `if link is None or link.tenant_id != request.user['tenant_id']: 404` — no created_by check; the listing GET /api/dashboard/share returns every link in the tenant to any member (created_by visible, token never serialised)",
  "reproduction": "code read; production GET /api/dashboard/share as tester -> 200 with the tenant's links (metadata only)",
  "evidence": "run 2: recorded from the route's body; triage round 2 batch 1: PROVEN by attempt on scratch — member 14 revoked 15's link (200) on the pre-change tree, 403 after the fix",
  "expected": "the creator or an admin revokes; a member sees their own links",
  "observed": "any member revokes anyone's link",
  "proposed_fix": "creator-or-admin on revoke; scope the listing to the caller unless admin; the authority table",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "verdict": "PROVEN by attempt (before: 200 on main @9fbe2e2; after: 403)",
  "closed_at": "2026-09-21T04:34:43+00:00",
  "closed_by": "triage-round2 batch 1 (authority completion), 2026-09-21",
  "closure": "PROVEN BY ATTEMPT on scratch (the table planted from production's DDL, read-only): on main @9fbe2e2 member 14 revoked admin 15's share link (200) and saw it in the listing; after the fix the revoke is 403 'Only the link's creator or an admin may revoke it', the listing is scoped to the caller's own links unless admin, the creator revokes their own and the admin revokes any — views.py api_dashboard_share_revoke / api_dashboard_share_list; both routes carry an entry in the route-authority table; tests/integration/test_authority_triage.py"
 },
 {
  "id": "AUD-040",
  "title": "CLAUDE.md says the public release-status endpoint answers 404 for 'no token / wrong token'; the route answers 401 for a missing token and 404 for a wrong one",
  "class": "documentation states a shape the code does not have",
  "severity": "low",
  "pass": "run2",
  "surface": "release/routes.py:290 (`if not token: 401 UNAUTHORIZED 'A status token is required.'`) vs CLAUDE.md 'Security posture'",
  "reproduction": "GET /api/releases/16/status -> 401; ?token=x -> 404 (scratch and production)",
  "evidence": "prod_render.json",
  "expected": "one documented contract",
  "observed": "two answers, one documented",
  "proposed_fix": "fix the doc (401 reveals nothing about the id; the oracle concern holds)",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-19T08:57:45+00:00",
  "closed_at": "2026-09-21T10:45:54+00:00",
  "closed_by": "round3 part B (housekeeping), 2026-09-21",
  "closure": "already corrected in round 2 (D-500 batch 1): CLAUDE.md reads 'no token → 401, wrong token → 404 (neither reveals whether the id exists)' and names releases.created_by as the owner rule. GUARD tests/unit/test_documented_contracts.py asserts the documented contract against the LIVE app (a missing token → 401, a wrong token → 404) and that the owner sentence names the column the routes read — RED on the audited tree 8990025"
 },
 {
  "id": "AUD-041",
  "title": "both waiver Revoke buttons have been dead since D-497: the forms sent a hidden EMPTY reason, which the service refuses ('a revocation carries its reason') — no waiver could be revoked from the product",
  "class": "a control that cannot be used (the D-497 rule closed the service; the forms were never taught the reason)",
  "severity": "medium",
  "pass": "round3",
  "surface": "primeqa/templates/settings/waivers.html and primeqa/templates/releases/_quality_decision.html: <input type=\"hidden\" name=\"reason\" value=\"\"> on the revoke forms; views settings_waiver_revoke / release_waiver_revoke → quality_policy.revoke_waiver refuses the empty reason (AUD-020)",
  "reproduction": "as admin on scratch: GET /settings/waivers → the revoke form's reason value is '' (proven 2026-09-21); submitting it → the flash 'a revocation carries its reason' and the row unchanged",
  "evidence": "the register's rendered form (name=\"reason\" value=\"\"); the decision tab's identical form; quality_policy.revoke_waiver's refusal since D-497",
  "expected": "a revocation with a reason a human types",
  "observed": "a button that always fails",
  "proposed_fix": "a required reason field on both forms",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T09:08:36+00:00",
  "closed_at": "2026-09-21T09:08:36+00:00",
  "closed_by": "round3 part A (what a pilot client hits first), 2026-09-21",
  "closure": "both forms carry a required reason input beside Revoke (no hidden blank); proven DB-real: a revocation through the register lands with its reason (revoked_by, revocation_reason); the form test fails on a hidden reason; RED on main; screenshots settings_waivers_active_lapsed_revoked.png, release_decision_revoke_with_reason.png"
 },
 {
  "id": "AUD-042",
  "title": "HTML entities double-escaped in two renders: the run page's 'Salesforce said' message shows &amp;#8377; where the org wrote &#8377; (the rupee sign); the landing page's flaky-test list shows '&middot;' literally between outcomes",
  "class": "escaping: an entity built in Python is escaped again by the template",
  "severity": "low",
  "pass": "round3",
  "surface": "GET /runs/562682d5-518e-4eaf-989c-ad0d23f91e92 (production, read-only render 2026-09-21): 'Loans over &amp;#8377;50,00,000 require Manager Approval'; GET / (production): 'errored &middot; errored &middot; passed …' under Flaky tests",
  "reproduction": "render the two pages over production data (D-487 renders, docs/ui-testing/round3-fixtures/PRODUCTION_run_detail_legacy_unstamped.png and PRODUCTION_landing.png)",
  "evidence": "the two screenshots",
  "expected": "₹ and · rendered",
  "observed": "the entity source text rendered",
  "proposed_fix": "unescape the org's message once at the read (html.unescape) and build the flaky sequence with the character, never the entity; a template test over a message carrying an entity",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T09:08:36+00:00",
  "note": "round 3 part E: reproduced OFF production — the shape is planted in the hostile tenant and the truth asserted, marked xfail(strict) against this finding, so it cannot pass quietly while the defect stands and cannot stay marked once it is fixed (tests/unit/test_template_escaping.py, tests/integration/test_round3_pages.py)",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part C, 2026-09-22",
  "closure": "the org's message is unescaped ONCE at the read (claim_presentation.org_rejection_message, html.unescape on both return sites); the flaky list joins with the character '·' in markup terms; the escaping gate (tests/unit/test_template_escaping.py) is the gate now — safe-marked expressions exempt — and the round-3 strict marker is gone; production's '&amp;#8377;' becomes '₹' (tests/unit/test_org_message_unescaped_once.py)."
 },
 {
  "id": "AUD-043",
  "title": "a claim title on the landing page carries a raw relative-date object: 'Automatically sets PLS FB Order SLA Deadline to {'$relative_date': {'anchor': 'RUN_DATE', 'offset_days': 5}}'",
  "class": "a value object rendered as its Python repr",
  "severity": "low",
  "pass": "round3",
  "surface": "GET / (production, read-only render 2026-09-21) — Recent Runs; the title comes from the claim's asserted value through claim_presentation",
  "reproduction": "PRODUCTION_landing.png",
  "evidence": "the screenshot",
  "expected": "a rendered value ('run date + 5 days')",
  "observed": "the dict's repr",
  "proposed_fix": "render $relative_date values in the title phrasing (the anchor and the offset in words); a unit test over a relative-date asserted value",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T09:08:36+00:00",
  "note": "round 3 part E: reproduced OFF production — the shape is planted in the hostile tenant and the truth asserted, marked xfail(strict) against this finding, so it cannot pass quietly while the defect stands and cannot stay marked once it is fixed (tests/unit/test_template_escaping.py, tests/integration/test_round3_pages.py)",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part C, 2026-09-22",
  "closure": "a RelativeDate asserted value reads in words wherever a claim's value is rendered — 'the run date', '5 days after the run date', '1 day before the run date' (claim_presentation.relative_date_in_words, used by _literal before and inside a LiteralValue); the round-3 strict marker on the planted hostile-tenant page is gone and the page test passes; unit tests/unit/test_relative_date_in_words.py."
 },
 {
  "id": "AUD-044",
  "title": "the dead-link sweep ignores converter types, so a literal segment the router refuses matched a parameterised rule — it called the dashboard's two 'Pending Reviews' links alive while /claims/inbox 404s",
  "class": "self-reporting control (the eighth): a sweep whose green is not evidence — the AUD-002 repair was incomplete",
  "severity": "medium",
  "pass": "round3",
  "surface": "scripts/deadlinks_gate.py::_rule_regex — every <...> became [^/]+, so '/claims/inbox' matched '/claims/<uuid:test_id>'; primeqa/templates/dashboard.html:59 and :69 link to /claims/inbox (the v1 review inbox, retired in 6a)",
  "reproduction": "GET /claims/inbox as admin on scratch → 404 (no rule under /claims is parameterless); the repo sweep on main reports 0 dead; with converters honoured it reports both dashboard links dead",
  "evidence": "the sweep's own report before and after (266 targets: 0 dead before, 2 dead after, 0 after the repair); the live 404",
  "expected": "a sweep whose alive means the router will answer; a landing-page card that reaches the reviews",
  "observed": "two dead links on the first screen after login, hidden by the gate that exists to find them",
  "proposed_fix": "match segment by segment with the converter's own pattern; repoint the links at the Requirements 'Needs review' tab",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T10:45:54+00:00",
  "closed_at": "2026-09-21T10:45:54+00:00",
  "closed_by": "round3 part B (housekeeping), 2026-09-21",
  "closure": "deadlinks_gate resolves a target against a rule SEGMENT BY SEGMENT, each parameter tested with its converter's pattern (int, float, uuid, path, string) and a Jinja wildcard satisfying any of them — so a rule mixing an int and a uuid still resolves and nothing cries wolf; the dashboard's two links point at /requirements?tab=needs-review (200). GUARD: two plants in tests/unit/test_no_dead_links.py — a converter-refused literal of each kind must be reported dead (RED on main), and mixed-converter / real-uuid / real-int targets must stay alive. The repo sweep: 266 targets, 0 dead"
 },
 {
  "id": "AUD-045",
  "severity": "medium",
  "class": "quarantined test (round 3 part C): red, listed with its finding, owner and mechanism, never silently skipped",
  "pass": "round3",
  "title": "the generation suite's seeded governance world no longer yields drafts: 26 tests answer REFUSAL where they expect DRAFT",
  "surface": "tests/integration/generation/test_automation_vertical.py (25), test_repair_agent.py (1) — the suite's own local governance database, seeded by its conftest",
  "reproduction": "pytest tests/integration/generation/test_automation_vertical.py → 25 failed, 43 passed; every failure is OutcomeKind.REFUSAL == OutcomeKind.DRAFT",
  "evidence": "fails identically alone and in the tree, and identically on main @2d18726 — not contamination, not introduced by round 3",
  "expected": "either the seed grounds what the governance core now demands, or the refusals are correct and the expectations are stale",
  "observed": "every archetype refuses",
  "proposed_fix": "AK's call: re-seed the vertical, or accept the refusals and rewrite the expectations against the current grounding rules",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T12:06:08+00:00",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part B, 2026-09-22",
  "closure": "SEED STALE, generation right. Since D-318 the state-transition and automation-effect resolvers bind a Flow by the EFFECT its parsed Metadata produces (recordUpdates assignments / recordCreates / the IR's update_records op); the D-210 seed's Flows carried no Metadata, so every claim refused EMISSION_DEFERRED 'no org automation produces Order__c.Status__c=Activated on create'. The seed's three Flows now carry the effects the vertical asserts (Stamp_Order_Status: Status__c=Activated + an Order_Log__c create; Escalate_Order: the same-record effect; Log_Effects: a filtered update_records on the parent reached from start). 24 of the 26 flipped to DRAFT on the seed alone; two refusal-text expectations moved to the current (earlier, stricter) reasons: the absence lane's 'verified trigger pair' and the placeholder value's 'verifiable effect'. The repair-agent round trip had two more product rules its world never showed — the repair gate SWITCH (tenant_agent_settings.repair_gate_apply_enabled, a public table the harness never carried) and AUD-013's plan on the re-run — both planted now. A REAL LLM generation on scratch was NOT possible: scratch holds zero connections (no LLM credential) and no production-shaped S1 (one seeded entity per org); reported as such. Vertical 68 passed, generation tree 215 passed."
 },
 {
  "id": "AUD-046",
  "severity": "medium",
  "class": "quarantined test (round 3 part C): red, listed with its finding, owner and mechanism, never silently skipped",
  "pass": "round3",
  "title": "the semantic suites' expectations drifted from the live schema: bitemporal intervals, enrichment counts and a rule's parse state (11 tests)",
  "surface": "tests/integration/semantic/test_delta_reconcile_live.py (6: _valid_to() is None where a closed interval is expected), test_s1_sync_console.py (4: count_failed_enrichment 0 vs 1; a phase reads 'complete'), test_validation_rule_field_refs.py (1: 'partial' vs 'unparsed')",
  "reproduction": "each file alone reproduces its failures",
  "evidence": "identical on main @2d18726",
  "expected": "expectations that match the schema and seed as they are",
  "observed": "three drifts of the same kind",
  "proposed_fix": "re-derive each expectation from the current behaviour, or fix the behaviour where the expectation is the contract",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T12:06:08+00:00",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part C, 2026-09-22",
  "closure": "EXPECTATIONS STALE, product right: all three suites scoped their planted rows by the pre-per-org column last_synced_from_org_id while the reconcile, the enrichment counters and the org-status compute read connected_org_id (D-323..D-325); rows were never 'this org's', so nothing closed and nothing counted. The plants set connected_org_id now (44 passed). The parse-state case: REGEX(...) is PARSED since the D-384 arc (a read reference to Name), so it cannot stand for 'unparsed'; an unknown function (FOO(Name)) does."
 },
 {
  "id": "AUD-047",
  "severity": "medium",
  "class": "quarantined test (round 3 part C): red, listed with its finding, owner and mechanism, never silently skipped",
  "pass": "round3",
  "title": "pgvector dimension mismatch on scratch: the column takes 1024 dimensions, the embedding the code produces is 1536 (6 tests)",
  "surface": "tests/integration/test_phase2_summaries_smoke.py (4), test_phase2_entities_sync_ai_smoke.py (2)",
  "reproduction": "pytest tests/integration/test_phase2_summaries_smoke.py → psycopg2.errors.DataException: expected 1024 dimensions, not 1536",
  "evidence": "identical on main",
  "expected": "one dimension across the column, the model and the fixtures",
  "observed": "two",
  "proposed_fix": "read PRODUCTION's vector dimension first (read-only): if production is 1536 this is a scratch-fidelity gap and scratch's column is repaired; if production is 1024 the embedding model changed under the schema and that is a product defect",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T12:06:08+00:00",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part B, 2026-09-22",
  "closure": "SCRATCH RIGHT, suites stale. Production read-only: every vector column is vector(1024) and every stored vector is 1024 (entities 31,943 non-null, flow_details 73, validation_rule_details 125; pgvector 0.8.2); scratch is 1024 too; the product embedder is voyage-3 at 1024 (intelligence/embeddings.py). The two suites hand-built 1536-dim literals tagged openai/text-embedding-3-small — the schema's FIRST shape, replaced by migration 20260514_0010 (D-049). They build 1024 and name voyage-3 now: 19 passed. No product defect."
 },
 {
  "id": "AUD-048",
  "severity": "low",
  "class": "quarantined test (round 3 part C): red, listed with its finding, owner and mechanism, never silently skipped",
  "pass": "round3",
  "title": "the staleness-pin suite expects GO where the policy now recommends CONDITIONAL_GO (4 tests)",
  "surface": "tests/integration/test_representation/test_step_b_staleness_pin.py",
  "reproduction": "pytest that file → assert 'conditional_go' == 'go' x4",
  "evidence": "identical on main",
  "expected": "the suite asserts the recommendation the current policy produces",
  "observed": "the recommendation the policy produced before D-488/D-496",
  "proposed_fix": "confirm CONDITIONAL_GO is right for the planted shape, then move the expectation",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T12:06:08+00:00",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part C, 2026-09-22",
  "closure": "CONDITIONAL_GO WAS RIGHT for what the suite planted: the decision's freshness check (window 168h) read the fixture runs at 382h old ('Newest run is 382h old') — fixed calendar dates that rotted. The fixture dates are now-relative with their order kept; the suite's GO expectations hold (6 passed)."
 },
 {
  "id": "AUD-049",
  "severity": "low",
  "class": "quarantined test (round 3 part C): red, listed with its finding, owner and mechanism, never silently skipped",
  "pass": "round3",
  "title": "the stranded-record cleanup counts 0 where the suite expects 1 and 2 (4 tests)",
  "surface": "tests/integration/execution_engine/test_stranded_cleanup.py",
  "reproduction": "pytest that file → assert 0 == 1, assert 0 == 2",
  "evidence": "identical on main",
  "expected": "the cleanup reclaims what the suite plants",
  "observed": "it reclaims nothing — either the planted shape no longer matches the reaper's predicate, or the reaper stopped working",
  "proposed_fix": "decide which, in that order; a reaper that reclaims nothing is a product question",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T12:06:08+00:00",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part B, 2026-09-22",
  "closure": "THE REAPER IS NOT BROKEN; the world it was shown was. (1) The D-245 Phase 4 guard reads the env's run policy from public.environments and reaps only a full, non-production env — the governance harness carries the tenant schema only, so the guard's SELECT failed, every env was 'unresolvable' and 0 was reaped (the guard working, the suite blind to it). (2) Round 4's write-ahead rule: a created record names a run that exists. The suite shims the guard's table (created only when absent), plants its runs and its environments, and asserts the guard by name: a read_only env, a production env and an env the tenant does not have are never reaped (13 passed). Production read-only: see the round-4 transcript for the stranded backlog by env."
 },
 {
  "id": "AUD-050",
  "severity": "low",
  "class": "quarantined test (round 3 part C): red, listed with its finding, owner and mechanism, never silently skipped",
  "pass": "round3",
  "title": "the two Step-5 suites cannot meet their preconditions on a database where the seed policy has been used (3 tests)",
  "surface": "tests/integration/test_representation/test_step_5_policy.py (2: 'a waiver's expiry lies in the future'), test_step_5_scope.py (1)",
  "reproduction": "pytest each file",
  "evidence": "the seed policy on scratch was activated and first used on 2026-09-15; D-488's immutability trigger then refuses any change to it, so the old precondition ('the seed is a draft') is unrecoverable",
  "expected": "a suite owns the policy it exercises",
  "observed": "both borrowed the migration's seed",
  "proposed_fix": "round 3 gave test_step_5_scope its own DRAFT version (it now reaches the composer, where recommendation is None instead of no_go — one step from green); test_step_5_policy needs the same treatment plus a waiver whose expiry the service accepts",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T12:06:08+00:00",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part C, 2026-09-22",
  "closure": "CALENDAR ROT + a transactional-world seam. The policy suite's NOW was a fixed date (2026-09-10): a waiver planted at NOW+7d fell into the past and the service refused it, rightly; NOW is live and the one calendar literal in an assertion is derived. The scope suite reached the composer, which reads its two pre-conditions over its OWN tenant session (D-492/D-496) — invisible to a world planted inside the suite's rolled-back transaction, hence scope_empty; the test points that read at its session, the composer's logic untouched (8 passed)."
 },
 {
  "id": "AUD-051",
  "severity": "low",
  "class": "quarantined test (round 3 part C): red, listed with its finding, owner and mechanism, never silently skipped",
  "pass": "round3",
  "title": "seven single-test reds of five unrelated kinds, each a stale expectation rather than a defect",
  "surface": "test_release_run_prod_gate.py (D-494's plan refusal answers before the SEC-4 tier message the suite greps for), test_ci_webhook.py (409 PLAN_REQUIRED vs 400 — the D-494 fork AK ruled on), test_phase5_authoring.py (asserts tenant_1 only; scratch has carried tenant_2 since the audit), test_r2_superadmin.py + test_auth.py (a custom in-file runner returns False and names no assertion), test_report_slice.py::test_a_runs_list_carries_both_recorded_runs (the 50-row window artefact), test_step_1_identity.py::test_1b_every_row_key_equals_the_pre_072_derivation (a DECORATED requirement is a legitimate exception the assertion does not admit)",
  "reproduction": "each file alone",
  "evidence": "identical on main @2d18726",
  "expected": "each expectation matches the product as it is",
  "observed": "each predates a ruling that changed it",
  "proposed_fix": "one small slice: move the four stale expectations, convert the two custom runners to pytest so they can say what failed, and page or scope the report-slice read",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T12:06:08+00:00",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part C, 2026-09-22",
  "closure": "seven stale expectations moved to the rulings that changed them: the release-run route refuses EVERY post with the plan requirement since D-494 (SEC-4 is enforced at the dispatch chokepoint; the route's code after the refusal is dead — recorded); the webhook's valid request reaches 409 PLAN_REQUIRED (the fork AK ruled); the cust store is asserted in a tenant schema, never public, for every tenant; the report listing's contracts are 'available, newest-first' and a run reads by its own id (the 50-row window was never a contract); a DECLARED identity key is admitted by the derivation test. The two custom runners are plain pytest now and NAMED their reds at once: R2 needed a superadmin (scratch's seed admin is an admin) and two checks whose module retired with v1 (primeqa.runs.cost); auth met TENANT_CAP because a crashed run's 15 fixture users filled the cap — the suite sweeps its own fixture shape at setup (15 passed, residue 0)."
 },
 {
  "id": "AUD-052",
  "severity": "medium",
  "class": "a test suite that does not clean what it plants",
  "pass": "round3",
  "title": "six integration suites leak rows into the scratch tenant on every run: ~540 claims, 10 claim sets, 9 inventories, 12 entities and 1 release per pass",
  "surface": "tests/integration/test_3a4_processor.py (claims 1, sets 1, inventories 1), test_3a5_entities.py (72/1/1 + 2 entities), test_phase7_comparison.py (144/2/2 + 4), test_prod_vault.py (144/2/2 + 2), test_tenant_isolation.py (1 release), test_ui_schedules.py (1 claim set)",
  "reproduction": "snapshot tenant_1's counts, run each file, snapshot again (the deltas above, measured 2026-09-21); the whole tree twice in one process adds 1,083 claims, 20 claim sets, 18 inventories, 24 entities and 3 releases",
  "evidence": "scratch's accumulated debris is the sum of every past run: 414 claim sets and 362 inventories for a tenant that has approved a handful",
  "expected": "a suite leaves the database as it found it, so the tree's second run starts where the first did",
  "observed": "each of the six plants an inventory, a claim set and its claims through the real services and removes only its S5 rules",
  "proposed_fix": "a remover per suite (or one shared marker-based remover), mindful that some of these tables carry never-delete triggers by design — where a trigger refuses, the suite should plant inside a transaction it rolls back instead",
  "note": "round 3 part C proved the OTHER half: the whole tree run twice in one process gives identical results (1055 passed, 185 skipped, exit 0 both times). Zero residue is NOT met and this finding is why",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-21T12:06:08+00:00",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part C, 2026-09-22",
  "closure": "every suite cleans up what it plants: one remover (tests/integration/_ui_world.py::remove_claim_set_world) takes a UI claim-set world down from the leaves up — comparison transitions and runs, verdicts, processing runs, inspection results and jobs, manifests by payload->>'claim_set_id', schedules, link claims, members and their claims across every test-id-keyed table, the sets, the inventory members and inventories, the materialised surface entities, and the materialisation's logical version (named by the inventory NUMBER, which is MAX+1 and reused — the row had to go with the inventory, and so did any entity that version still owned). test_3a4/test_phase7 remove by the ids their world holds; test_3a5/test_prod_vault use a 'new since the test began' teardown (the services they drive commit); test_ui_schedules removes its module's set; test_tenant_isolation had been SWALLOWING its own cleanup failure (`except Exception: rollback()`) and now fails loudly; the authority-triage world sweeps its own shape at setup (five TRIAGE-SURF worlds from crashed runs were on scratch); the boundary-gate and repair-gate worlds plant the run their proposal names (the round-4 key). Measured per suite in its own process: before +1 / +72 / +144 / +144 / +1 release / +1 set → after 0 for every one. THE PROOF, both halves: the whole tests/integration tree twice in ONE process — 1,255 passed / 15 skipped / exit 0 both times (identical), and every residue count identical before and after (claims 23,359, sets 448, inventories 392, entities 528, releases 4, requirements 0, users 6, environments 3, groups 3, connections 0, waivers 0, policies 1, proposals 0). Scratch was swept once of what earlier measurements and fifteen past tenant-isolation runs had left."
 },
 {
  "id": "AUD-053",
  "title": "the Remove-target button (release page) and the Unlink-surface button (requirement page) have been dead since D-497: both forms send a hidden EMPTY reason, which the planner (AUD-023) and the surface-link service (AUD-037) refuse before any write — no target can be removed and no surface unlinked from the product",
  "class": "a control that cannot be used (the D-497 rule closed the service; the forms were never taught the reason)",
  "severity": "medium",
  "pass": "round3-merge",
  "surface": "primeqa/templates/releases/detail.html:124 (<input type=\"hidden\" name=\"reason\" value=\"\"> on the /releases/<id>/targets/<env>/remove form) and primeqa/templates/requirements/_conformance_surfaces.html:87 (the same on the /requirements/<id>/surfaces/<link>/unlink form); views release_target_remove → planner.remove_target raises TargetAuthorityError('a removal carries its reason …'); requirement_surface_unlink → surface_links.unlink raises SurfaceLinkError(REASON_NO_REASON)",
  "reproduction": "read at the round-3 merge pre-flight (2026-09-22): the window grep for hidden empty reason inputs showed 4 on origin/main and 2 remaining after AUD-041's fix — these two; the services' refusals read in the code (planner.py remove_target, surface_links.py unlink). Not yet reproduced by submitting the forms.",
  "evidence": "the two rendered forms; the two service refusals; production read-only: release_targets 0 rows, requirement_surface_links 0 rows, no activity_log act naming a target removal or an unlink in 30 days — nothing has ever been removed or unlinked through the product, so the dead buttons have had no caller yet",
  "expected": "a removal / an unlink with a reason a human types (the AUD-041 shape: a required reason input beside the button)",
  "observed": "two buttons that always fail with the service's refusal flashed",
  "proposed_fix": "a required reason field on both forms, the AUD-041 pattern; a form test that fails on a hidden reason input on ANY form whose action route requires a reason (the class guard AUD-041 did not add)",
  "status": "closed",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-22T02:20:00+00:00",
  "note": "found while proving AUD-041's window on the deployed tree; recorded, not fixed — a UI change lands only after AK sees it (the standing rule), and the approved round-3 screens did not include these pages. Both buttons render only for MEMBER+ and only when a target / a link exists; production holds none of either.",
  "closed_at": "2026-09-22T00:00:00+00:00",
  "closed_by": "round4 part C, 2026-09-22",
  "closure": "the Remove-target form (releases/detail.html) and the Unlink-surface form (requirements/_conformance_surfaces.html) carry a required reason input (minlength 3) — the AUD-041 shape. CLASS GUARD tests/unit/test_no_hidden_empty_reason.py: a template sweep that fails on ANY hidden input named reason with an empty value, in any template — RED on main naming exactly the two forms, shown able to fail on a planted one while ignoring a visible reason field and a prefilled hidden one. Both pages are in the screenshot gate."
 },
 {
  "id": "AUD-054",
  "title": "the release-run route's body after its D-494 refusal is dead code: the SEC-4 production branch (Admin-only, explicit confirmation) is unreachable from this route",
  "class": "dead code behind a ruling (the AUD-005 class)",
  "severity": "low",
  "pass": "round4-part-C",
  "surface": "primeqa/views.py release_run: `flash(PlanRequiredError.REASON); return redirect(...)` precedes the environment checks and the SEC-4 branch, which can never run",
  "reproduction": "read the route: every POST returns before the code below; tests/integration/test_release_run_prod_gate.py now asserts the refusal for every caller",
  "evidence": "the route body; the round-4 rewrite of the SEC-4 suite",
  "expected": "a route body that ends where its behaviour ends; SEC-4 enforced at the dispatch chokepoint (it is: _authorize_dispatch, tests/unit/test_authz_dispatch_gate.py)",
  "observed": "~30 unreachable lines",
  "proposed_fix": "delete the dead branch (no behaviour change); or, if AK wants the route to run a plan, that is a new slice",
  "status": "open",
  "fix_plan_ref": null,
  "discovered_at": "2026-09-22T00:00:00+00:00",
  "note": "recorded while moving the SEC-4 suite's expectation (AUD-051); not deleted in round 4 — a change to a route body is not a test fix"
 }
]
