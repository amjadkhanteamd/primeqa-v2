# Technical audit — September 2026 (FIND ONLY, stopped at the first critical)

**Status: STOPPED at pass 4, attack #8, per the brief's rule** — "if a finding
is CRITICAL (… an invariant broken in pass 4) — STOP and report it immediately
rather than finishing the sweep." Nothing was fixed. Branch `audit-findings`
holds `docs/audit/` and `scripts/audit/` only.

Findings: **26** — 2 critical, 3 high, 12 medium, 9 low (13 at the first HOLD; 13 more on resume) — in
`docs/audit/findings.json`. Every finding names a CLASS (its shared cause);
ten classes, listed in §4.

## 1. Why this audit exists, and what it was told to hunt

No human has used the product for a task. Every verification to date proved
what it thought to test, and every recent defect was a data shape nobody
planned. The brief named the system's characteristic failure — **a control that
reports its own correctness** — and gave four instances (the read-only guard,
the repair gate reading the LLM's confidence, the audit-less schedule toggle,
the batching comment the code lacked). It asked for the fifth.

**Found: AUD-008.** The requirement page's readiness handler is commented
"never breaks the page". Its `except` clause calls `logging.getLogger`, and
`logging` is not imported in `views.py`, so the safety net raises `NameError`
exactly when it is needed. Every day the net is not exercised, the comment
reads as true. Four more findings are members of the same class (§4).

## 2. Method

Sessions were four minted users on a scratch copy — viewer, member and admin of
tenant 1, member of a fresh tenant 2 — with CSRF primed like a browser, so a
POST proves the route's gate and not the CSRF layer. Production was touched
only by read-only GETs under the server-side read-only connection option, and
that guard was **proven before every production read** by attempting `CREATE
TABLE`, `INSERT` and `UPDATE` (each refused with `ReadOnlySqlTransaction`).

The tooling itself is in `scripts/audit/` and was built to distrust
self-reports:

| tool | what it does, and the self-report it replaces |
|---|---|
| `inventory.py` | route census from the LIVE `url_map`, not the AST — the AST oracle misreads aliases (AUD-001) |
| `deadlinks.py` | every `href`, `action`, `hx-*`, `data-*` and `fetch()` target resolved against the live map, Jinja collapsed, `{% if %}` branches expanded — the unit sweep skipped every parameterised href (AUD-002) |
| `render.py` | every GET route rendered per session; asserts a `<main>` with text, the nav hooks, and never the 500 page; a fresh client per request because `/logout` poisoned the first run |
| `post_all.py` | every write route × session × five hostile bodies (empty, maximal, wrong-type, missing-CSRF, missing-required) |
| `session.py` | the minted sessions and CSRF priming |
| `ledger.py` | append-only findings with the brief's fields; `findings.json` is written from it |

Two of my own tools misreported during the audit and were caught by their own
evidence: a scripted edit that printed "applied" having matched nothing, and a
`pyflakes` run that reported zero undefined names because pyflakes was not
installed. Both are recorded here because they are the failure class under
audit, in the auditor.

## 3. Coverage — what was and was not done

| pass | item | state |
|---|---|---|
| 1a | dead links, every template + JS, every attribute | **done** — 166 targets, 5 dead, 2 on live pages |
| 1b | dict-method-key class in every template | **done** — 0 hits (the existing sweep is adequate for this class) |
| 1c | every GET route rendered per tier | **done on scratch** (324 renders × 4 sessions); the production read-only render was started, then stopped at the critical. Scratch is a substrate-first copy: seven public tables/columns are absent there (`groups.updated_at`, `milestones`, `llm_usage_log`, `shared_dashboard_links`, `behaviour_facts`, `failure_patterns`, `generation_quality_signals`), so their routes' 500s on scratch are recorded as **not covered**, not as findings |
| 1d | every form POSTed with five hostile bodies | **done on scratch** — 650 requests; 86 routes skipped for want of an id (plan, waiver, proposal, schedule, group, connection, requirement, section) — the hostile world of pass 2 was to supply them |
| 1e | orphan routes and templates | templates **done** (6 orphans); orphan ROUTES (reachable, nothing links) **not done** |
| 1f | self-reporting controls, PROVEN / UNPROVEN / FALSE | **partial** — 732 property-claim lines collected, 165 load-bearing ones triaged into `control_claims`; the ones this audit PROVED or found FALSE are in §5; the rest are UNPROVEN and listed nowhere yet |
| 2 | data-shape hostility on a hostile tenant | **not started** (tenant 2 provisioned empty; one zero-state finding fell out of it: AUD-007) |
| 3a | tenant isolation under HTTP | **not done** (the tenant-2 member rendered 324 routes without reaching tenant-1 data in any 200 body, but the id-swapping sweep was not run) |
| 3b | capability at the route (viewer POSTing to act routes) | **done on scratch for routes with ids** — 165 forbidden, 152 redirected to `/`, zero act routes reached; 86 routes skipped for want of an id |
| 3c | secrets in output | **not done** |
| 3d | IDOR: acting on a visible-but-not-yours resource | **one finding** (AUD-012: any member mints/rotates/revokes any release's poll token); the sweep was not run |
| 4 | the nine invariant attacks | **#8 run and SUCCEEDED (AUD-013 — the stop)**; #3 (browser worker emits a verdict) PROVEN impossible by grep — `primeqa/browser_worker/` contains no writer of `s6_interpretations`; #1, #2, #4, #5, #6, #7, #9 **not run** — their mechanisms were located (§5) but not attacked |

Two attempts at #8 were refused before the third succeeded, and both refusals
were for the WRONG reason — environment scope, then "no executable recipe".
Neither refusal mentioned a plan. That is why the attack was pressed rather
than recorded as refused.

## 4. Class summary

| n | class | members |
|---|---|---|
| 3 | retirement removed a symbol whose callers survived | AUD-006 (share links, high), AUD-009 (Jira search, high), AUD-010 (legacy metadata, low) |
| 2 | dead link on a live page (link retired, anchor kept) | AUD-003 (release "View Reasoning", medium), AUD-004 (sections "View TCs", low) |
| 1 | self-reporting control: an invariant enforced at some entry points and asserted for all | **AUD-013 (critical)** |
| 1 | self-reporting control: a safety net whose own body cannot run | **AUD-008 (high) — the fifth instance** |
| 1 | self-reporting control: the gate oracle reads decorator names, not the decorators that run | AUD-001 (medium) |
| 1 | self-reporting control: documentation claims a gate the route does not enforce | AUD-012 (medium) |
| 1 | a sweep that skips what it cannot parse reports the residue as clean | AUD-002 (medium) |
| 1 | documentation asserts a component the code no longer wires | AUD-005 (low) |
| 1 | a data shape nobody planned: the zero-state of an aggregate | AUD-007 (medium) |
| 1 | no validation at the boundary — the database column is the validator | AUD-011 (medium) |

Five of thirteen are self-reporting controls. Three are the retirement's
survivors — the same cause as the `plan_id`-never-selected defect the brief
cited. The retirement commit removed definitions; it did not run an
undefined-name check; `pyflakes` finds all eight sites in under a second and is
not a gate.

## 5. Self-reporting controls with a verdict

| control | claim | verdict |
|---|---|---|
| `views.py:2982` readiness handler | "never breaks the page" | **FALSE** — the handler raises `NameError` (AUD-008) |
| D-486 / intake | "every run starts from a RECORDED plan" | **FALSE at the chokepoint** — the API and claim routes enqueue without one (AUD-013) |
| CLAUDE.md, status token | "Mint + revoke are Release-Owner/admin" | **FALSE** — MEMBER floor, no owner check (AUD-012) |
| `scripts/authz_inventory.py` | the D-245 regression oracle | **FALSE for aliases** — three gated routes reported open (AUD-001) |
| `test_step_6a_routes.py` dead-link sweep | "every internal href … must match a rule" | **FALSE by construction** — hrefs with Jinja are excluded (AUD-002) |
| CLAUDE.md component kit | `feedback_modal.html` + `tc_feedback.js` | **FALSE** — rendered by nothing, calls a dead API (AUD-005) |
| `release_substrate_console` docstring | "not N read_claim_runs calls" | was FALSE until D-493; now true — recorded as the fourth instance's closure |
| `/shared/<token>` docstring | "action buttons are stripped in the read-only template" | **PROVEN** — the template holds zero forms/buttons (and the route 500s anyway, AUD-006) |
| `record_waiver` | "no expiry, no waiver" | PROVEN in the service (`expires_at is None → PolicyError`); the DB CHECK `expires_at > created_at` passes NULL — **the column's nullability was not read; UNPROVEN at the DB** |
| `quality_policies_immutable` trigger | a used policy refuses edits | mechanism located (`20260912_0010`), 10 refusals recorded at D-488 — **not re-attacked here** |
| `requirement_identities_immutable` trigger | key columns refused once established | mechanism located — **not attacked** |
| `execute_plan` | one execution per plan | `REFUSE_ALREADY_EXECUTED` located — **not attacked** |
| `promote_claim_to_approved` | humans-only actor | **PROVEN incidentally** — `actor='audit'` was refused with `AuthorityViolationError` during pass 4 setup |
| browser worker | never emits a verdict | **PROVEN by absence** — no writer of `s6_interpretations` in `browser_worker/` |

## 6. Scratch state left behind

Tenant 2 (`AUDIT-t2`, schema `tenant_2`, user 12) and three tenant-1 audit
users (13 viewer, 14 tester, 15 admin) remain on scratch for the sweep's
continuation. One approved claim with an active metadata recipe
(`75b381c5…`) was planted for attack #8; the job it enqueued (131) was
cancelled. Twenty-eight public migrations were applied to scratch for fidelity;
thirty-one cannot apply because scratch never held the v1 tables.

## 7. What resumes on GO

Pass 2 (the hostile tenant), pass 3a/3c/3d, the orphan-route half of 1e, the
remaining 1f verdicts, and pass-4 attacks #1, #2, #4, #5, #6, #7 and #9 — in
that order, with `post_all.py` re-run once the hostile world supplies the 86
missing ids. The production read-only render (1c) should be re-run to
completion; it was stopped mid-way by the critical rule.

---

# Resume — 2026-09-15 (passes 2, 3, 4), STOPPED again at a critical

The audit resumed after slices 1 and 2 (D-494, D-495) closed AUD-006/008/009/010/013.
**Total findings now 26 — 2 critical, 3 high, 12 medium, 9 low.** This resume
STOPPED at the second critical (AUD-014) under the same rule.

## The two criticals

- **AUD-013** (contained in slice 1, merged D-494): an execution enqueued with
  no plan. Closed before the resume.
- **AUD-014** (this resume, pass 4): **Evaluate records a GO decision over zero
  checks.** A release whose only declared target is an inactive environment, and
  a release with no scope at all, both had `POST /releases/<id>/evaluate-decision`
  write a `release_decisions` row with `recommendation=go` and `criteria_met`
  true on all six axes — the policy graded an empty input as satisfied. The
  release board says "Evaluate will refuse" for the same release. Two invariant
  attacks the brief named (get GO on an unattested scope; write a decision from
  a refused evaluation) both succeeded here.

## Pass 2 — the hostile tenant

`scripts/audit/hostile_world.py` planted every shape the brief named on scratch
(586 claims incl. one requirement of 79 and one of 500 drafts, all-deprecated,
a revoked claim set, a superseded inventory, unstamped runs, a policy with no
rules, a release with no scope, a release scoped to an inactive environment, a
claim with no coverage, a surface declared then unlinked, an expired waiver, a
plan never executed, a plan executed twice) and a manifest-driven `remove`. Every
page rendered against it; counts checked, not just status. Findings: AUD-014
(critical), AUD-015 (expired waiver vanishes), AUD-016 (bad run id → 200 "store
unavailable"), AUD-017 (today's unstamped run mislabelled "predates stamping"),
AUD-018 (`revoked` claim-set status has no writer), AUD-022 (all-deprecated
requirement reads never-tested). The zero-state finding AUD-007 stands from the
first pass.

## Pass 3 — isolation, capability, IDOR, secrets

- **3a tenant isolation: HOLDS.** `scripts/audit/isolation.py` — a tenant-2
  member drove all 108 id-taking routes with tenant-1 ids: zero tenant-1 markers
  in any body, zero 2xx on any act. The eight 200s were not-found shells or empty
  lists (AUD-024: those should be 404).
- **3b capability at the route:** the 41 previously-skipped POST routes ran with
  the world's ids. Viewers reached no act route (`/ask` and `/run` answer for a
  viewer by design). Members reached acts they should not: AUD-019 (policy
  activation at MEMBER), AUD-020 (revoke another user's waiver, empty reason),
  AUD-023 (remove another user's release target). AUD-011 gained a member
  (`PATCH /api/requirements/<id>` 500 on oversize).
- **3c secrets in output: NONE.** `scripts/audit/secret_scan.py` over 276
  rendered bodies (three tiers), all four service logs, and the connection /
  environment / auth API JSON per tier: zero hits. Connection secrets are
  admin-only (viewer/member get 403 on the detail route) and `config` is not
  serialised.
- **3d IDOR:** AUD-020 and AUD-023 are the ownership gaps; decision finalisation
  correctly refused a non-owner.

## Pass 4 — the nine invariant attacks

| # | attack | result |
|---|---|---|
| 1 | a PASS verdict for a check that did not run | **SUCCEEDED** — AUD-026 (no FK on `s6_interpretations.run_id`; readers are saved only by their join) |
| 2 | GO on a release with unattested scope | **SUCCEEDED** — AUD-014 (critical) |
| 3 | the browser worker emits a verdict | refused by construction — no writer of `s6_interpretations` in `browser_worker/` |
| 4 | change a claim identity | refused — `requirement_identities_immutable` trigger |
| 5 | apply a SPECULATIVE / SEMANTIC repair | **NOT RUN** — the sweep stopped at AUD-014 |
| 6 | edit a used policy | refused — `quality_policies_immutable` trigger |
| 7 | a waiver with no expiry | refused — NOT NULL on `expires_at`, and the route shows "no expiry, no waiver" |
| 8 | enqueue a run bypassing the planner | contained in slice 1 (D-494) |
| 9 | write a decision from a refused evaluation | **SUCCEEDED** — folded into AUD-014 |

## Coverage now

Done: passes 1 (all), 2, 3a/3b/3c/3d, 1e orphan routes, pass-4 attacks 1/2/3/4/6/7/8/9.
**Not done: pass-4 attack #5 (speculative/semantic repair apply)** — stopped at
the critical. The 1f self-reporting verdicts are extended by pass 4 (the four
immutability/refusal invariants PROVEN by attempting them; two — the plan
invariant and the empty-scope grading — found FALSE).

## Scratch left behind

The hostile world is removed (manifest-driven, plus a psql autocommit sweep for
the rows the `except: pass` trap had left — that trap is itself recorded: a
failed Postgres statement aborts the whole transaction, so `except Exception:
pass` around per-table deletes commits nothing). Zero residue: 0 AUD links,
requirements, releases, policies, plans, orphan verdicts, non-terminal jobs.
`test_step_1_identity` passes again. Tenant 2 and audit users 13/14/15 remain
for a future resume. `.claude/launch.json` carries no temporary entries.

---

# Run 2 — 2026-09-19: the audit finished. FINDINGS ONLY.

The two earlier passes stopped at criticals (AUD-013, AUD-014; contained and
closed by D-494 and D-496). Run 2 was told to finish the unfinished half
without stopping: a critical would be CONTAINED in one commit and the sweep
would continue. **No critical was found in run 2.** No containment was needed.
Branch `audit-run2` from `main` @8990025 (after D-497), tooling and findings
only, pushed at every milestone. **Total findings: 40 — 2 critical (both
closed), 4 high (3 closed, 1 open), 20 medium (8 closed, 12 open), 14 low
(3 closed, 11 open); 24 open, 16 closed.**

## Pass 2 — the hostile tenant, built once and rendered at all three tiers

`scripts/audit/hostile_world.py` (v2) planted every shape the brief named on
scratch tenant 1 — zero-requirement release; 500 draft claims on one
requirement; one requirement with 79 approved; five claims all deprecated; a
revoked claim set; a **superseded inventory (minted for the first time this
run — the earlier plant's member shape was wrong and the note said so)**;
runs with no stamp; a policy with no rules; a release with no scope; a
release scoped only to an inactive environment; a claim with no coverage
rows; **a surface declared then unlinked (minted for the first time — the
requirement needed an established identity)**; an expired waiver; a plan
never executed; a plan executed twice (the second refused:
`plan_already_executed`); **a requirement whose identity is a decorated
non-Jira key (external_key `AUD-IDENT`, jira_key `AUD-JK-1` — the AUD-014
latent shape)**; **orphan run_id rows in the three sibling tables (the
AUD-028 shape) — and the proof that `s6_interpretations` now refuses one
(the D-497 FK held: `IntegrityError`)**. The remover commits statement by
statement under the replica role; residue 0 after every remove.

**Rendered: every GET route (99) × viewer, member, admin = 297 renders, 0
exceptions, 0 skipped.** 500s: 36 renders, every one a scratch-fidelity gap
(`groups.updated_at`, `failure_patterns`, `shared_dashboard_links`,
`milestones`, `behaviour_facts`, `llm_usage_log`,
`generation_quality_signals` — absent on scratch), so those routes were
rendered **on production, read-only, at all three tiers: 99 renders, 0
error pages** (§ item 6 below). No new 500 on the hostile shapes.

**The numbers (`scripts/audit/counts2.py`, 25 checks × 3 tiers):** the
79-claim requirement counts 79 on its list row and lists its claims on its
page; the 500 drafts count 500 and the nav badge carries them; the
decorated-identity requirement counts its 2 test cases on the list, the
page, the board and the decision tab (the page and the act derive the same
key since D-496 — the latent shape is closed); the no-scope and
inactive-only releases refuse on the board and the card with one sentence
(D-496); the never-executed plan reads "not yet run", the twice-executed one
"run by audit admin · 1 job"; the revoked claim set reads revoked; the
zero-rule policy is listed; the no-coverage claim reads "Not assessed".
**Silently wrong numbers, confirmed on the v2 world: AUD-022** (the
all-deprecated requirement's list row reads "no tests yet · Generate →"
while its page lists five deprecated), **AUD-015** (the expired waiver is
absent from the waivers page at every tier), **AUD-016/024** (the orphan run
id renders a 200 "Run not found" shell at every tier). The "? unclassified"
origin the hostile rows show is a planter artefact (direct inserts establish
no identity; the app's create and generation paths do — production's eleven
live requirements all carry one).

## The 86 skipped POST routes — now zero skipped

`scripts/audit/post_all.py` with the world's ids: **1,620 requests** (every
write route × viewer/member/admin × empty, maximal, wrong-type,
missing-CSRF, missing-required), **1,080** with a tenant-1 session on
**tenant-2 ids** (the foreign-id variant; a tenant-2 world was planted for
it), **540** as the tenant-2 member on tenant-1 ids. Skipped rules: **0**.

- **Cross-tenant writes: none.** Every tenant-2 row was read before and after
  the foreign-id run: unchanged; no API answer was 2xx on a tenant-2 id; the
  tenant-2 member reached no tenant-1 act (its 200s are `/ask` and the
  plan-refused `run-async` fragment, which reveal nothing).
- **Viewers reached no act route** (`/ask` answers for a viewer by design).
- **500s:** 39 (own), 24 (foreign), 2 (t2). Scratch-fidelity: 36 (`groups.*`,
  `failure_patterns`, `behaviour_facts`). Real: **AUD-011 gained six routes**
  (oversize → `StringDataRightTruncation` on POST/PATCH `/api/releases`,
  POST `/releases`, PATCH `/api/environments/<id>`, PATCH
  `/api/sections/<id>`, PATCH `/api/requirements/<id>`; wrong-type →
  `InvalidTextRepresentation` on POST `/api/releases/<id>/requirements`) and
  **AUD-035** (section purge with children → `ForeignKeyViolation` 500).
- The sweep mutates the world it is given (it deleted the hostile release
  and requirement; it ran the never-executed plan as member 14). Counts are
  read before it, and the IDOR pass re-planted.

## Orphan routes and templates

`scripts/audit/orphans.py` over the repaired link graph (every template
attribute, htmx verb, form action, JS literal, `navigation.py` entry,
Python `redirect()`; other Python string literals counted as WEAK references
only): **211 rules, 112 linked, 80 orphan — 71 are the JSON API surface
(AUD-034: no in-product caller; 35 auth-only GETs, 18 MEMBER writes, 29 ADMIN
writes), 9 are non-API**: `/sections` (a live page nothing links to,
**AUD-031**), `/suites` + `/suites/<id>` (redirect to the retired `/claims`
→ 404, **AUD-032**), `POST /plans` and `POST /requirements/<id>/run-substrate`
(orphan acts, **AUD-033**), `/health`, `/shared/<token>` (external by
design), `/claims/<id>/panel` and `/environments/<id>/sync-substrate/status`
(polled by JS with a built URL — linked, invisible to a literal scan),
`/results/<int:run_id>` (a legacy redirect). Gates were read from the
decorators' CLOSURES (a `Tier` or a role tuple held in a cell), never from
names. **Orphan templates: 4** (AUD-005, narrowed by D-497): `claims/inbox`,
`components/_legacy_banner`, `dashboard_release`, `run/index`; no dynamic
`render_template` call exists.

## IDOR completion — see, but do not act

`scripts/audit/idor.py`: member 14 with VALID bodies on objects user 15 owns,
the row read before and after; the viewer and the tenant-2 member on the
same acts.

| act on 15's object, as member 14 | row | verdict |
|---|---|---|
| mint / revoke the status token on 15's release | changed both ways | **AUD-012 confirmed** (open) |
| finalise 15's decision as NO GO (not more permissive) | `final_decision` NULL → no_go, decided_by 14 | **AUD-036** — the API route is admin-only, the web route MEMBER: two gates for one act |
| cancel 15's generation job | unchanged, 403 "Only the creator or an admin can cancel" | PROVEN owner check |
| unlink a surface 15 declared | `active` true → false, "72 conformance checks removed from the plan" | **AUD-037** — the AUD-023 shape on surface links |
| run 15's never-executed plan | executed, `executed_by` 14 | by design (any member executes any plan) — noted |
| edit 15's requirement; rename 15's release; remove a requirement from it | changed | by design (requirements and releases are shared, not owned) — noted |
| approve 15's revoked claim set | unchanged | refused on state |
| viewer: finalise / mint / edit | unchanged, 302 `/` or 403 | PROVEN gate |
| tenant-2 member: finalise / mint / edit tenant-1's | unchanged, 404 or `/` | PROVEN isolation |

Code-read only (no table on scratch, no production writes): **AUD-039** —
`POST /api/dashboard/share/<id>/revoke` checks the tenant, not the creator.

## The self-reporting sweep — the seventh instance

923 property-claim lines (`never` 444, `refuse` 112, `cannot` 75,
`verified` 61, `safe` 52, `best-effort` 50, `always` 47, `gated` 42 …) were
collected; the load-bearing controls were attempted. Verdicts, cumulative:

| control | claim | verdict |
|---|---|---|
| read-only production guard (SET-based) | reads only | **FALSE** (D-488 era; replaced by the server-side option, proven every run since) |
| repair-proposal confidence | gates apply | **FALSE** (Step A; the verdict gates now) |
| schedule toggle | audited | **FALSE** (D-486 fixed) |
| batching module comment | one query | **FALSE** (D-489) |
| requirement page readiness net | "never breaks the page" | **FALSE** (AUD-008, fixed D-495) |
| pre-commit hook | runs pyflakes on staged Python | **FALSE for its entire life** (AUD-027, fixed D-497, proven red) |
| **`users.is_active`** | a deactivated user is out | **FALSE — the seventh (AUD-038)**: the flag is never read on a request; the API deactivate path bypasses the service's revocation; a deactivated session read and WROTE for the access token's 30 minutes |
| fail closed at boot in production without a real `JWT_SECRET` (app.py:40, core/auth.py:16) | refuses to start | **PROVEN** — unset and the dev default both refuse (`SecretConfigError`) |
| CI webhook without `WEBHOOK_SECRET` (release/routes.py:412) | 503, never a signature check against nothing | **PROVEN** |
| a GET deep-link to `/ask` (views.py:413) | never auto-runs an LLM call | **PROVEN** (the gateway stubbed to raise; 0 calls) |
| the status endpoint without a minted token (release/routes.py:293) | 404 for every token | **PROVEN for a wrong token; the doc's "no token → 404" is 401** (AUD-040, low) |
| the repair tick over a proposal whose run does not exist (scheduler.py:216) | never an error in the tick | **PROVEN** on the AUD-028 shape |
| generation-job cancel (views.py:4663) | creator or admin | **PROVEN** by attempt |
| tenant scoping of every user mutation (core/service.py:170) | invisible → not found | **PROVEN** (540 + 1,080 foreign-id requests, 0 cross-tenant writes) |
| the four immutability triggers, the waiver NOT NULL, the D-497 FK / CHECKs / apply trigger | refuse | **PROVEN** by attempt (pass 4, D-497 on production) |
| `claim_sets.status = 'revoked'` | a state | **FALSE as a producer claim** — no writer (AUD-018) |
| the unstamped run's sentence | "predates stamping" | **FALSE** for a run made today (AUD-017) |
| `environments.is_active` in the release scope | inactive is out | **PROVEN** (the inactive-only release refuses by name, D-496) |
| SEC-1 redaction of connection/environment config | never serialised | **PROVEN** (secrets sweep; production `/api/environments` as viewer: no config) |

Remaining UNPROVEN: the 900-odd lines not on this table — S3 generation's
refusal grammar (295 lines), S1 sync's best-effort wrappers (73), the
browser worker (33) — each a claim about a process this audit did not drive.

## Item 6 — the rest of the original brief

- **Pass 1c on production, completed:** the seven scratch-uncoverable routes
  plus the main surfaces, three tiers, read-only: 99 renders, 0 error pages;
  `/suites` → `/claims` → 404 confirmed live; `/api/dashboard/share` lists
  metadata only (no token serialised); a viewer's `/api/environments` and
  `/api/requirements` carry no secret.
- **Pass 1f:** the table above; the residue stays UNPROVEN and is named.
- **Pass 3c:** re-read on production for the routes above: no secret.
- **Pass 4:** all nine attacks now attempted (#5 by D-497's slice).

## Coverage statement

| covered | how | not covered, and why |
|---|---|---|
| every GET route × 3 tiers on the hostile world | 297 renders | the 7 fidelity-gap routes on scratch — rendered on production instead |
| the numbers on 25 shapes × 3 tiers | counts2 | numbers on pages only production data reaches (LLM usage, milestones, patterns) — read there, not asserted |
| every write route × 3 tiers × 5 bodies, + foreign ids both directions | 3,240 requests | bodies a browser could not send (multipart, raw JSON on form routes) |
| orphan routes and templates | the link graph | URLs built at runtime from data attributes (two polls, found by hand) |
| IDOR on the acts with an owner | 16 attempts | share-link revoke (no table on scratch); acts on production |
| the self-reporting controls that gate an act | 20 verdicts | ~900 claims in S1/S3/browser-worker prose: UNPROVEN |
| the nine invariant attacks | all attempted | — |
| tenant isolation | 1,620 foreign-id requests, rows read | the browser plane's job routes (no fixtures) |

## Class summary — 40 findings, 35 classes

The recurring ones: **self-reporting control** (7 members: AUD-001, 008, 012,
013, 027, 038, and the read-only guard of D-488); **guarded by convention, not
by constraint** (AUD-020, 023, 026, 028, 029, 037, 039); **no validation at the
boundary** (AUD-011 with seven routes, 021, 035); **a data shape nobody
planned** (AUD-007, 015, 022); **a lookup miss reported as a page** (AUD-016,
024); **reachable, unlinked** (AUD-025, 031, 033, 034); **retired path,
surviving reference** (AUD-003, 004, 005, 006, 009, 010, 032); **documentation
states what the code does not do** (AUD-005, 012, 040); **one act, two gates**
(AUD-036). Full list: `docs/audit/findings.json`.

## Scratch left behind

The hostile world removed (residue 0), tenant 2's rows removed, user 14
re-activated and its planted refresh token revoked, the tenant settings
dormant, the seed policy in the state this run found it. Tenant 2 and the
audit users 13/14/15 remain.
