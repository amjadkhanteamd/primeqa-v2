# Substrate 8 — Evolution Engine — Evolution Log

Append-only. One entry per session that made substantive changes to this substrate's docs or code.

---

## 2026-05-31 — Substrate opened; grounding-validity predicate + recipe-grounding leg (D-112 / D-113 / D-114-flagged)

Substrate 8 opened — the **evolution layer**: it governs whether a test remains *meaningfully true* as the org evolves. Faculty-first (mirroring how S6 opened): the semantic core opens; the evolution *mechanics* (manifests, triggers, reverse-index, re-grounding orchestration) are explicitly fenced (SPEC §6 / `DEFERRED_ITEMS.md`). SPEC + D-112 committed on `phase-5-substrate-4-crud` (PR #5, merged).

**The keystone (D-112).** Identity is org-independent *by construction* — `identity_hash` strips `version_seq` + `external_id` (the C0/C1 canonicalization invariants), so a rename / formula-edit / re-sync **cannot** change a claim's identity. Therefore org evolution is **never** an identity-axis event — **always** a grounding-axis event. S8 governs grounding continuity *under identity preservation*; it never changes what a test means (the S2 constitution already mandates identity-preserving-versions-only). One sentence: **S8 re-asks generation's grounding questions against the current org.**

The semantic core is a deterministic **grounding-validity predicate** `(artifact, current org) → intact / drifted / broken` — pure, on-demand, stateless, **two-level** (claim-grounding + recipe-grounding, which drift independently). Its legs are *generation's grounding checks, re-asked*: claim-grounding → S1 entity-resolution; recipe-grounding → S3 derivation; admissibility → S3 admissibility logic (an extensible set; a field-value-validity leg is hinted). **Dependency law:** `S8 → {S1 resolution, S3 derive, S3 admissibility}`, **parallel to S6, never S6 → S8** — the decisive reason is *epistemic* (S6 judges post-run with the actual run evidence; S8 judges pre-run by derivation; the stronger-evidence faculty must not route through the weaker). Plus the **supersession law**: re-grounding is an *identity-preserving supersession* — a new version with the **same** `identity_hash`, on the existing lineage spine, stamped `recipe_s8_rewrite` / `grounding_evolution`. Meaning is the invariant; grounding evolves.

**Slice 1 — the recipe-grounding leg (D-113).** `recipe_grounding_validity(payload, subject, *, s1) → intact / drifted / broken` (`primeqa/evolution/recipe_grounding.py`) — does a behavioral-negative recipe's stored payload still violate the current VR formula? Built on a **new neutral `formula.evaluate` primitive** (`primeqa/semantic/formula/eval.py`, Kleene three-valued) — *not* S3's `derive` (D-112's "S3 derive" line was approximate: the leg *evaluates* `formula + payload → fires?`; `derive` *solves* `formula → a violating assignment`). **Object-level** (the recipe pins no VR — only a generic `FIELD_CUSTOM_VALIDATION_EXCEPTION`): **intact** = ≥1 active VR's current formula evaluates `True`; **drifted** = none triggered but ≥1 active evaluable VR exists; **broken** (reason-tagged `no_active_vr` / `formula_non_evaluable`). Produce-only, no production caller (purely dormant). D-113 refined SPEC §2/§3; the `intact`-masks-drift limitation (an unrelated required-field VR firing) is the deferred VR-pin.

**Slice-1 follow-up (D-114, primarily in S6).** S6's `_attribute_not_enforced` was aligned to consume the shared `formula.evaluate` (off S3's `derive`), so the **parallel-siblings law is realized on `evaluate`** — its two consumers are S6 `attribution.py` (post-run) + S8 `recipe_grounding.py` (pre-run); `derive` returns to S3-internal. The **S8↔S6 `NonEvaluable`-symmetry pass** (align the leg's `False`+`NonEvaluable` mix to S6's conservative line → `broken/formula_non_evaluable` ahead of `drifted`) is **held** — grounded on frequency first (a probe found it marginal vs. the generation-side VR-pin, the real object-level fix). See `DEFERRED_ITEMS.md` §2.

---

## 2026-06-03 — Predicate legs + the in-substrate mechanics (D-139 … D-143)

**Phase 6 (program) — five slices.** The predicate is completed over the **realized** S1/S2/S3 surface, persisted, and wired to **fire** as the org evolves. The leg set the opening hinted (claim-grounding, field-value-validity) is built; admissibility is verified S3-blocked and deferred. On `phase-14-substrate-8-evolution`.

**Slice 1 — claim-grounding leg (D-139).** `claim_grounding_validity_for_claim(asserted_truth, *, s1)` — does the claim's subject still resolve? Re-asks generation's resolution step (`get_entities` by `sf_api_name`) through S8's own `SubjectResolver` port, walking **every** `IdentityBearingRef` in the body (claim-kind-agnostic; mirrors the D-058 §5.4 coverage walk, projected to `external_id`). **By external_id, not entity_id** — a rename supersedes the entity but changes the api-name execution addresses, so by-external_id catches the rename-break by-entity_id would mask. Two-valued (`intact`/`broken`, `subject_not_resolved`). Pure; produce-only.

**Slice 2 — field-value-validity leg (D-140).** `field_value_grounding_validity_for_recipe(recipe, *, s1)` — do the payload's field values still *exist*? Closes recipe-grounding's structural blind spot: `evaluate` returns `True` on a *removed* picklist value (the formula string-compares fine), so only an existence check on the active value set catches it. `PicklistReader` port (`active_values → frozenset | None=not-a-picklist`); `broken`/`picklist_value_removed` + the offending `(field, value)` pairs. Behavioral-negative-only v1; multi-select split deferred. Pure; produce-only.

**Slice 3 — the two-level composition (D-141).** `grounding_validity(artifact, *, subjects, vrs, picklists)` composes the three legs claim-level + per-recipe, **composed never collapsed** (Fork C — the parts stay addressable). Precedence `broken > drifted > intact`: a field-value `broken` **un-masks** a recipe-grounding `intact`; a claim-level `broken` dominates `overall`. Recipe legs are negative-only → non-negative recipes are skipped, not faked. Three ports (parallel-siblings preserved). Pure.

**Slice 4 — the recorded-verdict store (D-142).** A per-tenant `s8_grounding_validity` table (alembic tenant `20260603_0030`; PK `test_id`+`version_seq`=claim version; `evaluated_at_version_seq`=the S1 seq grounded against — the snapshot axis, S8-Q-007; `overall`/`claim_verdict` TEXT; `detail` JSONB; `overall` index) + `persist_grounding_validity` (an **UPSERT** — recompute refreshes) + `GroundingValidityRead` DTO + `read`/`list`. The thin mechanics (the D-137 parity); persist + read only.

**Slice 5 — the S1-sync recompute trigger (D-143).** `S8S1Reader` (the production tri-port over `SemanticOrgModel` — `resolves`/`vrs_for_object`/`active_values`, mirrors `interpretation/s1_reader.py`) + `recompute_grounding` (per current claim: **freshness** = a store row at the current S1 seq; stale → ground + persist, up to a per-tick `cap`) + `recompute_tenant_grounding` + `run_s8_grounding_tick`; wired into `scheduler.py`'s `scheduler_tick` as `s8_grounding_tick`. **No watermark table** — freshness is derived from the store (`evaluated_at_version_seq == current S1 seq`), which is **cap-correct** (a partial recompute resumes next tick). Recompute-all on S1 advance; the change→impact reverse index that would narrow it is deferred.

**Verification.** 37 unit (the legs + composition, pure) + 11 governance integration — `test_s8_s1_reader` (6, the tri-port against seeded S1) + `test_s8_grounding_store` (6, the store round-trip + UPSERT) + `test_s8_recompute` (5, the orchestration: grounds/skips-fresh/regrounds-stale/caps + the `load_current_artifacts` decode). App + scheduler import OK. **Deferred (tracked, `DEFERRED_ITEMS.md`):** admissibility (S3-blocked); the reverse index; re-grounding orchestration/supersession execution (autonomy-gated); the VR-pin; the NonEvaluable-symmetry pass; the multi-select picklist split. On `phase-14-substrate-8-evolution` (D-144 close).
