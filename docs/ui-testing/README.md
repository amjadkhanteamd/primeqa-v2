# UI testing — design of record + implementation record

**Gate 2 closed 2026-08-24 with FULL TA approval** for the Foundation/R1
substrate build; **Phase 3A is unlocked**. Two conditions attach to
PRODUCTIONISATION (not the build): **P-1** — a real customer-architecture
authenticated run before the first customer-authenticated production run;
**P-2** — stable egress before any allowlisting-dependent customer.
`docs/ui-testing/design/` holds the **signed design of record**
(HLD/SAD/TAD v1.2 + the Gate 2 return package + the TA decision verbatim);
the spike LLDs beside this file (`LLD_*.md` + `SPIKE_SCOREBOARD.md`) are
the **implementation record** of the Phase 2 spike that the design was
signed against. Ledger: D-460 (Gate 2 closure, worker interpretation
boundary mandatory) and D-461 (manifest invariant) in
`docs/architecture/DECISIONS_LOG.md`.
